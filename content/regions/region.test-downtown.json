{
  "$schema": "../schemas/region.schema.json",
  "id": "region.test-downtown",
  "name": "Downtown (Test)",
  "description": "A throwaway region used only to prove the M0 content loader + schema gate (T6). Not shipped content.",
  "baseline": {
    "threat": 40,
    "zombieDensity": 55,
    "loot": 70,
    "survivorActivity": 20,
    "power": 0,
    "water": 30
  },
  "adjacent": ["region.test-residential"],
  "notes": "Delete once M1 authors the real first region. Exists to exercise validate + index by id."
}
