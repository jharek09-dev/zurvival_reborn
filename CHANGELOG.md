# Changelog

All notable changes to the Zurvival Reborn design and repository are recorded here.
The format is loosely based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Design

- **Wireframe Kit v0.2 + Art Bible v0.2 — the design docs stop disagreeing with each other and grow production teeth (colorway untouched).** The wireframes now practice what the bible preaches: story prose renders in **Literata** (webfont-embedded, with JetBrains Mono), the type specimen mirrors the bible's §3.2 token scale exactly (`--type-display` 32/1.15 serif → `--type-micro` 12 mono), the Quiet Screen line and map annotations use the serif registers (Literata italic replaces the non-portable script face), and every screen carries a **stable SCR ID**. Four new annotated screens close the nav's promises: **SCR-09 Radio** (Steel monopoly, aging/decayed signals, broadcast as a priced risk), **SCR-10 Workshop** (honest text recipes, missing parts stated, repair-over-replace), **SCR-11 Encounter/Trade** (dialogue registers, real offer costs, trust moves after), and **SCR-12 Night/Power-out** (the same tokens one step dimmer — tone by subtraction, rendered live). The component library gains a **choice-row states card** (default/pressed/2px-ember-halo focus/trust-locked with the gate stated in words). The art bible adds the **SCR screen registry** (§5), a **26-concept canonical icon inventory** (§6.1), the **reproducible duotone recipe sidecar template** (§7.6), **empty/loading/error state copy** in the game's voice (§8), and a **+35% localization-expansion rule** (§3.7.7). `design/wireframes.html` + `design/art-bible.md`, both bumped to v0.2.

### Milestone

- **M4 — Part 11 (T55) is adaptive audio + non-audio equivalents — the world you can *hear*, delivered as text so the game is fully playable with sound off.** **FR-AUD-01/02/06** (AUDIO bible; GDD Part XVIII). In a text-forward game "the soundtrack is the graphics"; T55 adds a standing **soundscape** — the AUDIO bible's client-side **Audio Director** (§13.2) rendered as sparse sound-captions — so a distant shot, a nearing horde, a Screamer's shriek, the wrong late-night hush, and your own heart in your ears are all things you perceive and act on, and every one of them is **text** (FR-AUD-06, Must — the primary channel here, not a retrofit). Because AUDIO §13.3 makes audio downstream and side-effect-only (it reads the Scene, never writes `GameState`, draws no RNG, reads no clock — sound on and off must produce identical runs) and every fact already lives in the engine's public read API, the whole feature is a **harness-client** change — a new `prototype/harness/src/soundscape.ts` plus a `soundscape` region in `play.ts` and an optional `graph` threaded through `renderRegions`/`renderScene`/`transcript` — that **touches NO engine file, NO content, NO schema, NO save rung, NO RNG**. The determinism surface is **literally unedited** (the T54 shape, the strongest byte-identity story): `diff -r` of `prototype/engine/src` **and** `content/` against the pre-edit baseline are **both empty**, so every engine golden and the cross-tree `saveGame` proof hold **by construction**. **The five adaptively-mixed layers (§3), Golden-Rule sparse (§2.1):** an **ambient bed** (region × phase × weather × interior/shelter — weather is *informational*: rain/fog "pull the detail back"); **environmental** one-shots from node state (a fire's crackle, the dead, your barricades, a dead grid); the **dynamic/informational layer** (**FR-AUD-02**) — **noise located by direction & distance** over a breadth-first earshot (`[a sharp crack of sound — near, toward the Corner Store]`, fair-to-fog: a bearing is named only through a *discovered* neighbour, so a caption never leaks the map), the **seven zombie-type signatures** each with its §6.2 tell (a Rotter's wet drag, a Fresh's ragged sprint, a Crawler low and below, a Bloated's gurgle, the Riot's armour, the **Screamer's shriek** = "the whole area just woke", the Stalker's night wrongness), behavioural **state reads** (turning-toward-you / the chase tightening / feeding), and the **horde collective bed** sized and swelling by distance; the **player body** (**FR-AUD-02**) — the **heartbeat scaled by composited Fear** (the prototype's `mind.stress` raised by the nearest threat's real closeness, an active fight, a horde on your tile, darkness, wounds), breath, a footstep-surface cost you can hear, and the **infection distortion** surfaced by *stage* (§9.2) fairly (you are told your hearing can't be trusted, never fed a fabricated cue); and the **music/tone** (**FR-AUD-01**) — the Director's read as a theme word + 0–4 intensity (survival/exploration/danger/loss/home), **level-0 is silence** (no tone line — the heartbeat is the level-0 track). **No number ever leaks** (Fear a heartbeat in words, the sickness a symptom, proximity a distance word, the music a mood word). CI green in a clean cloud sandbox: **engine 561 (+0 — untouched) · content-loader 9 · harness 170 (+33) · typecheck clean ×3 · schema gate 160 entries / 13 types · malformed rejected · empty-turn smoke exit 0.** A **two-subagent adversarial audit — each with a follow-up VERIFY pass** (engineering: purity / determinism / byte-identity / crash-safety / no-number-leak — the purity/no-leak/no-forbidden-token claims *proven over an 882,000-state matrix*; design: FR-AUD-01/02/06 fidelity / every meaningful cue captioned / reachable+surfaced in real play / fairness of the unreliable-audio model / voice) surfaced **0 BLOCKERs + 7 findings, all fixed and re-verified**: the **heartbeat under-read danger** ("steady" through a chase, a fight, a horde on your tile — now reads real proximity + a combat floor), the **Danger theme was binary-pinned at Level 4** for any acute cue (now continuous, L4 reserved for a threat on top of you / a last stand), a **noise line that blamed the player** for external Screamer-cascade noise (reframed to honest "it's loud here"), a **non-urgency-aware sparsity cap** that could drop the most critical cue and mute the Fear/tone (the dynamic layer is now structured `Cue{text,urgency,proximity}`, sorted before the cap, and Fear/tone read the full pre-cap set), a horde-on-you that dropped its size, an own-node moan that double-stated a roused node, and `?? []` crash-hardening; the post-fix contradiction sweep is **0** (the heartbeat and the music can no longer disagree on a turn). New: `prototype/harness/src/soundscape.ts`, `prototype/harness/test/soundscape.test.ts`, `docs/plans/M4_PART11_PLAN.md`, `docs/qa/QA_REVIEW_M4_PART11.md`. Changed: `prototype/harness/src/{play,playCli,playSlice,index}.ts` (the `soundscape` region + optional-graph seam), `prototype/harness/gen-slice.ts` (passes the graph), `prototype/harness/test/accessibility.test.ts` (the FR-AUD-06 sound-off proof). **NO engine/content/schema change; save schema stays v10; the 14-stage pipeline is untouched.** **Deferred:** Dynamic Audio Memory (FR-AUD-05, needs the Quiet Screen — **PL-M4-49**); radio sonic identity per signal type (FR-AUD-04 — **PL-M4-50**); accessibility audio settings / caption-verbosity toggles (with the T56 settings surface — **PL-M4-51**); hearing-damage/tinnitus as a played mechanic (an engine concern — **PL-M4-52**); caption wording tuned at the M5 comprehension playtests. See `docs/plans/M4_PART11_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART11.md`.
- **M4 — Part 10 (T54) is depth screens on demand — inventory, companions, shelter, map/journal, codex — the drill-down layer over the one decision.** **FR-UI-04** (GDD Part XVII "Information on demand"; wireframes SCR-03..07). M4 has spent nine parts making the world behind the T19 story screen deep; T54 gives the player a way to *look at* that depth without cluttering the story. Because FR-UI-04 is a **presentation** requirement and every fact already lives in the engine's public read API, the whole feature is a **harness client** change — a new `prototype/harness/src/screens.ts` plus small seams in `play.ts`/`playCli.ts`/`playSlice.ts` — that **touches NO engine file, NO content, NO schema, NO save rung, NO RNG**. The determinism surface is **literally unedited**, the strongest possible byte-identity story: `diff -r` of `prototype/engine/src` against the pre-edit baseline is **empty**, so every engine golden and the cross-tree `saveGame` proof hold **by construction** (not "gated dark" — *untouched*). A depth screen is a pure `(state, graph) => string[]`; opening one **resolves no turn, spends no time, and mutates no state** (proven: rendering never changes `JSON.stringify(state)`; a screen key in `playByInputs` adds no turn and is recorded in a new `screensViewed` list; the transcript is byte-identical with or without screen keys inserted). **The five screens (SCR-03..07), each words-only, no bar, no number:** **Inventory** (`I`) — pack weight in words (room to spare / heavy / full-leave-behind), items grouped by category, **artifacts** with provenance (gated on real instance metadata, so a plain worn pistol isn't mislabelled), economy freshness + learned recipes; **Companions** (`C`) — "N with you · M at home", per-companion condition (needs in words, wound named, **infection a symptom never a number**), mood + **trust as a tier word not the 0–100 scalar**, and standing orders **read from the engine's own `companionOrderChoices` so the screen never promises an order the engine will refuse** — the withheld ones **locked with the real gate in words** (scavenge needs trust AND a base; guard needs trust); **Shelter** (`B`) — walls/morale/stores in words, built rooms vs. the **actually-unbuilt** "could build" room recipes, who's home + their job + the jobs the built rooms allow, and a **persisted daily report scanned from the append-only Living History** (what your absence cost); **Map & Journal** (`M`) — fog percentage, here/home, every known node with its memory (searched-state, walkers, discoveries), the player's **handwritten notes**, the fog edge, and recent auto-annotated history; **Codex** (`L`) — lore, the **radio gated on actually carrying a radio** (each signal's label/type + a readable status-plus-reception phrase), rumors (leads confided, mysteries), and the **memorial** — the dead (`†`) and the departed (`↳`) by name and by *how*, from the history — plus the run's moral shape felt, not counted. **Accessibility (extends the T20 baseline, NFR-ACC-01/02):** every screen a stable frame (title bar naming the active screen = the "pressed" state, back hint last), **zero ANSI**, keyboard-reachable by one mnemonic key each (`I C B M L`, none colliding with the reserved `S`/`Q` or the digit choices), and the persistent **footer advertises every key** so nothing is missable; the M4 component-library states map to text — **pressed** (active title bar), **focus** (`>` marker + words), **trust-locked** (gate stated in words, never a mystery grey). CI green in a clean cloud sandbox: **engine 561 (+0 — untouched) · content-loader 9 · harness 137 (+26) · typecheck clean ×3 · schema gate 160 entries / 13 types · malformed rejected · empty-turn smoke exit 0.** A **two-subagent adversarial audit — each with a follow-up VERIFY pass** (engineering: engine zero-drift / non-mutation / no-crash over degenerate & extreme states; design: FR-UI-04 fidelity / no-number-leak / **the loops are reachable AND surfaced in real play** / voice) surfaced **0 BLOCKERs** but **7 high-value findings, all fixed and re-verified**: three "dead affordance"/comprehension bugs — a companions **"ask them" lead teaser the engine can't fulfil** (the `ask` verb is for met NPCs, not companions; removed), a shelter **"Could build" that listed already-built rooms** (inverted `buildableJobs` selector → now driven by unbuilt room recipes), and a **scavenge order shown available with no base** (now derives from the engine's offer + words the base gate); the shelter **daily report never persisted** (now scans history); a humanity-line double-period/voice clash; plus polish (compound item names, artifact-label gating, capitalized memorial names, corrected companions footer) and two defensive `?? []` guards so a hand-built/corrupted state can't crash `renderMap`/`historyLine`/`renderCodex`. New: `prototype/harness/src/screens.ts`, `prototype/harness/test/screens.test.ts`, `docs/plans/M4_PART10_PLAN.md`, `docs/qa/QA_REVIEW_M4_PART10.md`. Changed: `prototype/harness/src/{play,playCli,playSlice,index}.ts` (the `{kind:"screen"}` Command variant, `parseCommand` routing, the registry-built footer, the free-overlay loop seam), `prototype/harness/test/accessibility.test.ts` (depth-screen discoverability + reachability). **NO engine/content/schema change; save schema stays v10; the 14-stage pipeline is untouched.** **Deferred:** FR-UI-06 emotional UI + the "Quiet Screen" (SCR-08 — **PL-M4-45**); in-screen actions/sub-navigation (T54 keeps the decision on the primary Scene per FR-UI-01 — **PL-M4-46**); a real lore/rumor content set for the codex (**PL-M4-47**); surfacing a companion's authored leads post-recruit (**PL-M4-48**); screen wording/ordering tuned at the M5 comprehension playtests. See `docs/plans/M4_PART10_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART10.md`.
- **M4 — Part 9 (T53) is factions & inter-NPC relationships — the game's soul — plus the long-deferred off-screen people-sim.** **FR-NPC-02/05/06/07** (GDD Part XII). A new `engine/src/sim/social.ts` interprets `content/factions/*.json` **generically** (the T47/T50/T51/T52 idiom) over a new transient **`graph.factions`** pool; the seam (`socialChoices` / `isSocialAction`+`resolveSocialAction` / `socialLine`) wires into `coreActions` like jobs. **`socialActive(graph)` (a faction pool is registered) is the MASTER gate** — dark without one, so **every prior run is byte-identical** (proven **cross-tree** against the pre-T53 baseline across **13 seeds** with encounters/radio/recipes/jobs all active + forced threaten/give paths + `advanceWorld` jumps: **byte-for-byte equal, RAW, no normalization** — stronger than T51, because **T53 adds NO save-schema rung (stays v10)**: the new survivor axes are optional/tolerated-absent, written only when social is active, so nothing is seeded at spawn). **NO new RNG stream** (desertion/movement/leads all deterministic — thresholds + BFS + first-unshared), **NO loot-table growth** (leads reveal nodes / mark discoveries, never a `floor(f·len)` pick set), **NO new item**. **FR-NPC-02 — memory → per-relationship trust/respect/fear (not a global bar):** beside T34's `trust`, a survivor now carries **`respect`** + **`fear`** (0–100), moved by a **bounded, append-only `memory`** (`MEMORY_CAP 12`) of what you did; `trust` still moves through T34's `applyTrustEvent` (every T34/T35/T36 test holds), respect/fear/memory are the **gated overlay** on `give`/`threaten` — and are **surfaced as behaviour, never a number** (`attitudeRead`: afraid / deferring / contempt, in `peopleLine`). **FR-NPC-06 — conversations that hint:** `NPCDef.knowledge` authored leads (`npc.schema` extended, additive) — `ask` a met, trusting survivor and the lead **REVEALS a real node (fog-lift)** or **marks a discovery**; **5 leads on cass/walt/sarah, all reveal real, NON-adjacent, lootable nodes** (grand-pharmacy, union-bank, the strip-mall gun shop, the garden center, Mercy's pharmacy wing) — listening pays in **world state**, tracked by a `told:` flag on `player.flags` (no rung), re-checking co-location. **FR-NPC-05 — desertion & betrayal (deterministic, hours-based `DESERT_HOURS 18` pressure — chunk-invariant):** `moraleTarget` reads the leader's **humanity** (PL-M4-15's reader, at last) **AND individual mistreatment** — **NEGLECT** (a companion's own unmet needs, weighted ×1.5), **TERROR** (fear carried from menacing them before recruit — `recruit()` now carries respect/fear/memory), **DISTRUST** (severe neglect **erodes companion trust** — now **bidirectional**, the reachable low-trust path). A worn-down companion **DESERTS** (a legible `companionUnease` tell shows first — fairness); a malicious (`respect ≤ 20`), terrified (`fear ≥ 85`), or cruelly-led (`humanity ≤ 28`) one **BETRAYS** — robs the base stash. A deserter/betrayer is **never logged as `companion.died`** (`history.ts` guards on the `left.<id>` flag). **FR-NPC-07 — inter-NPC bonds → shelter morale:** recruited co-faction survivors seed a friendly `relationships` bond, rivalries a hostile one; **shelter morale** (aggregate resident `mind.morale`) moves with who's home — surfaced as `shelterMoodRead` (close-knit / on-edge, old grudges / wearing down). **3 factions** (harbor-holdout, quad-collective, slagworks-crew) over the 18-survivor cast, **two recruitable cross-faction rivalries** (hector↔sarah, dana↔marcus) that actually bite. **Off-screen people-sim (closes PL-M3-02 / PL-M4-35):** `advanceWorld` now drifts non-party survivor needs (they can starve in a district you skip), drifts the party (once — guarded vs the jobs path), advances morale/desertion, and **stage-10 `moveGroups` GRADUATES** from its reserved `identity` no-op to a gated `tickGroups` (survivors regroup toward faction home via deterministic BFS) — the **14-stage names/order unchanged** (`pipeline.test`). **Content — a new schema-validated type:** `content/schemas/faction.schema.json` + **3 factions** take the gate **12 → 13 types**; `npc.schema.json` gains an additive optional `knowledge`. CI green in a clean cloud sandbox: **engine 561 (+28) · content-loader 9 · harness 111 (+5) · typecheck clean ×3 · schema gate 160 entries / 13 types · malformed rejected · empty-turn smoke exit 0.** A **two-subagent adversarial audit — each with a follow-up VERIFY pass** (engineering: determinism / byte-identity / save / forged-edges; design: FR fidelity / no-number-leak / the loops *actually close in real play* / voice) surfaced **0 BLOCKERs** but a set of high-value "present-but-not-reachable/surfaced" findings, **all fixed and re-verified**: a false `companion.died` beat on desertion/betrayal; **betrayal was dead code** (respect never fell → wired `menaced-me` to lower respect + carry axes across recruit + a reachable cruel-leader path); **desertion only read global humanity** (added neglect/terror/distrust individual drivers); **two hollow `marks`-only leads + Walt's lead pointed at a node adjacent to where you'd ask him** (all re-authored as `reveals` of hidden nodes); **respect/fear were write-only** (now surfaced); **shelter morale was never shown** (now surfaced); per-tick desertion pressure (now per-hour, chunk-invariant); and the inert `dana↔gus` rivalry (gus never recruits) swapped for the recruitable `dana↔marcus`. New: `engine/src/sim/social.ts`, `engine/test/social.test.ts`, `content/schemas/faction.schema.json`, three `content/factions/*.json`, `docs/plans/M4_PART9_PLAN.md`, `docs/qa/QA_REVIEW_M4_PART9.md`. Changed: `engine/src/state/types.ts` (optional `respect`/`fear`/`memory`/`desertPressure` + `SocialMemory` — no rung), `engine/src/sim/{npcs,encounters,companions,worldSim,history}.ts` (the `knowledge` field, the give/threaten overlay, recruit carries the axes, the off-screen people tick, the `left.` death-guard), `engine/src/pipeline/applyAction.ts` (stage-5 `tickPeople`, stage-10 `moveGroups` graduated), `engine/src/actions/coreActions.ts` (the seam + `peopleLine` attitude/mood reads), `engine/src/map/{types,regionGraph,seedWorld}.ts` (the `graph.factions`/`graph.people` pools + `seedFactions`), `engine/src/index.ts`, three `content/npcs/npc.{cass,walt-pearson,sarah}.json` (authored leads), `harness/{src/playCli.ts,test/content.test.ts}` (registers the pool + faction/knowledge content tests). **NO save-schema rung (stays v10); the 14-stage pipeline order is unchanged.** **Deferred:** social dials (M5 balance, **PL-M4-40** — incl. the morale-drift-vs-need-clock rate, sub-threshold rivalry magnitude, and a bounded desertion-timing granularity); the `node.discoveries` reader so a `marks` lead pays out (extends PL-M4-17, **PL-M4-41**); the FR-NPC-09 Storyteller + FR-NPC-08 personal quests/romance + FR-NPC-10 full faction diplomacy (**PL-M4-42**); consequential faction reputation + `groupId` wiring (**PL-M4-43**); respect's own reachable behavioral consequence (**PL-M4-44**). See `docs/plans/M4_PART9_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART9.md`.
- **M4 — Part 8 (T52) is shelter depth: jobs & craftable rooms — the base becomes a second loop that runs on its own.** **FR-SHL-03/04** (GDD Part XI). A new `engine/src/sim/jobs.ts` interprets `content/jobs/*.json` **generically** (the T47/T50/T51 idiom — no per-job branching) over a new transient **`graph.jobs`** pool; the seam (`jobChoices` / `isJobAction`+`resolveJobAction` / `jobLine`) wires into `coreActions` like economy, gated on an active job pool **and** (for assignment) standing in your own shelter with a room built and a companion present — inert otherwise, so **every prior run is byte-identical** (proven **cross-tree** against the pre-T52 baseline across **12 seeds**, 60-turn playthroughs incl. two off-screen `advanceWorld` jumps: **byte-for-byte equal, no normalization needed** — T52 adds no save field on a pool-less run; an adversarial subagent independently reproduced it over 638 compared steps / 44 off-screen jumps, 0 mismatch). **FR-SHL-03 — assignable jobs on your turns and away:** assign a resident companion (a free 0-hour `assign-job` verb, the T45 order idiom — a **`job:<id>` flag on the `Survivor`, so NO save-schema rung, stays v10**) to a room's job; `tickShelterOps` runs it in **pipeline stage 5** (beside `tickCompanions`) **and** in **`advanceWorld`**, so a played hour and a fast-forwarded hour do the same. **Six jobs / four shapes:** produce-only (garden → `item.food-fresh`), consume→produce (kitchen fresh→canned, infirmary cloth→bandage, salvage → scrap), **hold-power** (generator burns `item.fuel` to raise `world.powerGrid`), and **upkeep** (watch keeps the shelter's barricades up). A job short of its input **stalls** (never drives the stash negative); a hold-power/upkeep job at a full grid/wall stalls **before** burning its input. **FR-SHL-04 — craftable rooms that unlock capability:** five new shelter recipes (garden/kitchen/watchtower/generator/radio) **reuse the T51 `recipe.schema.json`** (`installsRoom`, no new room type), and every workable room now *does* something — workshop/medical add `job.salvage`/`job.infirmary` to their T51 crafting; garden/kitchen/generator each unlock a job; kitchen is **the fridge** (stash fresh food doesn't spoil while it stands); watchtower **halves off-screen barricade decay** + unlocks `job.watch`; radio **shields a broadcast** (a shielded broadcast drops the loud node deposit — **PL-M4-25**). **Closes PL-M3-01** — the base **feeds its residents** from the stash (fresh food first, **before** the drift/death pass, so a stocked base keeps people alive without touching the pack). **Closes PL-M3-05 / lands the PL-M4-08 off-screen half** — `advanceWorld` (gated on `jobsActive`) drifts the party + runs their orders, runs the jobs, feeds the residents, and **decays the barricades** (halved under a watchtower, held by a posted watch); a material off-screen loss surfaces a `shelter.weakened` note so decaying defenses are never silent (GDD XI). **Closes PL-M4-29** — the **fridge** spoils stash `item.food-fresh` → `item.food-spoiled` only when the grid is failing **and** neither a kitchen nor a running generator keeps it cold, and the **generator** burns fuel to hold `world.powerGrid` up (keeping the fridge cold and the T51 carried-food clock slow). **No new RNG stream, no loot-table growth** — jobs move only *existing* items between the stash and the world, so the `floor(f·len)` hazard T50/T51 had to gate never arises. **Content — a new schema-validated type:** `content/schemas/job.schema.json` + **six jobs** take the gate **11 → 12 types** (five new room recipes ship under the existing recipe type). CI green in a clean cloud sandbox: **engine 533 (+36) · content-loader 9 · harness 106 (+7) · typecheck clean ×3 · schema gate 157 entries / 12 types · malformed rejected · empty-turn smoke exit 0.** A **two-subagent adversarial audit** (engineering: determinism / save / **byte-identity of every prior golden** / no-underflow / no-double-decay / forged-assignment edges — it *proved* the 638-step cross-tree byte-identity; design: FR-SHL fidelity / no-number-leak / the loops actually close / voice) surfaced **0 BLOCKERs + 7 findings, all fixed**: the daily report read "*food fresh*" (fixed to "fresh food"); a double period on every room build (`economyLine` strip); the promised **`job.watch`/upkeep shape was cut** (restored — the watchtower is now postable); **off-screen wall decay was silent** (now a `shelter.weakened` note); the generator burned fuel at a full grid (now stalls); an imperative people-line ("set to tend the garden"); and a `jobLine` robustness fix (the report now wins over a stale assign beat when `advanceWorld` reuses a turn number). New: `engine/src/sim/jobs.ts`, `engine/test/jobs.test.ts`, `content/schemas/job.schema.json`, six `content/jobs/*.json`, five `content/recipes/recipe.shelter.{garden,kitchen,watchtower,generator,radio-room}.json`, `harness/test/jobs.test.ts`, `docs/plans/M4_PART8_PLAN.md`, `docs/qa/QA_REVIEW_M4_PART8.md`. Changed: `engine/src/actions/coreActions.ts` (the seam + `peopleLine` job read), `engine/src/pipeline/applyAction.ts` (stage-5 shelter ops, feed-then-drift), `engine/src/sim/worldSim.ts` (the off-screen base tick), `engine/src/sim/companions.ts` (`withOrder` clears a job flag), `engine/src/sim/economy.ts` (room-build line period fix), `engine/src/sim/radio.ts` (radio-room shielded broadcast), `engine/src/map/{types,regionGraph,seedWorld}.ts` (the `graph.jobs` pool), `engine/src/index.ts`, `harness/src/playCli.ts` (registers the pool). **NO save-schema rung (stays v10); the 14-stage pipeline order is unchanged.** **Deferred:** the full off-screen people-sim (PL-M3-02/PL-M4-08 remainder, T53 — **PL-M4-35**); sub-cycle granularity (PL-M2-04 — **PL-M4-36**); the job/room dials (M5 balance — **PL-M4-37**); morale/friendships from shared labour + trading the surplus (FR-NPC-07/FR-ECO-08, T53 — **PL-M4-38**); a base raided live (PL-M3-06/08 — **PL-M4-39**). See `docs/plans/M4_PART8_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART8.md`.
- **M4 — Part 7 (T51) is the economy: resource loops, spoilage, crafting, repairs — one content-driven system that closes all four resource loops.** **FR-ECO-04..07** (GDD Part X/XI · wireframe **SCR-10**). A new `engine/src/sim/economy.ts` interprets `content/recipes/*.json` **generically** (the T47/T50 idiom — no per-recipe branching) over a new transient **`graph.recipes`** pool; the seam (`economyChoices` / `isEconomyAction`+`resolveEconomyAction` / `economyLine`) wires into `coreActions` like radio, gated on an active recipe pool **and** (for the bench verbs) standing in your own shelter with the parts — inert otherwise, so **every prior run is byte-identical** (proven against the pre-T51 baseline across **13 seeds**: no loot draw, inventory item, region-loot value, or rng-stream state shifted). **The four loops each get a real sink:** **body** — food spoilage (`item.food-fresh` ages on the single `economy.freshness` clock, **faster below `POWER_SPOIL_AT 40`** — read from `world.powerGrid`, T27 — turning to `item.food-spoiled` at 0; canned food never spoils) *and* fresh food is now **eatable** (better food than a can — a real "eat it now before it rots vs hoard the shelf-stable can" decision, the germ of "the last can"); **safety** — an equipped **durability artifact wears** with each melee strike and is restored only by **repair**; **health** — crafted bandages / antibiotics; **power** — boiling water and running the bench debit `item.fuel`. **Crafting** (medical/weapon/shelter/survival) is gated by **blueprints** (learned from a found `item.blueprint.*` via `study`), **components**, and **rooms** (a shelter recipe `installsRoom`, another `room`-requires) — all folded into one `RecipeDef`; the workshop listing **states every missing part** (SCR-10), never a greyed mystery. **Repair-over-replace** (FR-ECO-07): a repair restores a carried artifact's durability and **appends a provenance line to its `metadata`** — the instance id never changes, and the prose reads as the artifact's story growing ("its third repair … it carries the marks now"), not a stat bump. **Purification** (FR-ECO-05): `item.water-dirty` is never drinkable directly; a `purify` recipe (boil = fuel, or a charcoal-and-cloth filter) is the only path to safe `item.water`. New lootable economy items are appended to the tables **only when the economy is active** (`includeEconomy`, the `includeRadio` discipline — a `floor(f·len)` pick shifts on a table growth). **No new RNG stream** — every economy option is a deterministic conversion. **SAVE SCHEMA v9 → v10** — one forward-only rung (`migrateV9toV10`) seeds `player.economy = {blueprints:[], freshness:null}` + every node `rooms: []` at their inert empties, so a migrated pre-economy save is byte-identical. **Content — a new schema-validated type:** `content/schemas/recipe.schema.json` + **11 recipes** across all six categories take the gate **10 → 11 types**. CI green in a clean cloud sandbox: **engine 497 (+26) · content-loader 9 · harness 99 (+6) · typecheck clean ×3 · schema gate 146 entries / 11 types · malformed rejected · empty-turn smoke exit 0.** A **two-subagent adversarial audit** (engineering: determinism / save / **byte-identity of every prior golden** / forged-action edges — it *proved* the 13-seed baseline byte-identity; design: SCR-10 fidelity / no-number-leak / FR-ECO coverage / voice) surfaced **1 BLOCKER** — fresh food was findable and spoilable but **could never be eaten**, so the body sink was only nominal (fixed: `eat` now reaches fresh → canned → spoiled, canned behaviour unchanged so the golden path stays byte-identical) — **+ 8 findings, all fixed** (a forged `study` learning a blueprint free; an already-built room recipe re-crafting and burning inputs; a forged repair on a full artifact; a mint-id collision; a "boil" line on the charcoal filter; three inert crafted outputs whose prose over-promised; the missing-part surface reporting only the first shortfall). New: `engine/src/sim/economy.ts`, `engine/test/{economy,economyMigration}.test.ts`, `content/schemas/recipe.schema.json`, eleven `content/recipes/*.json`, `harness/test/economy.test.ts`, `docs/plans/M4_PART7_PLAN.md`, `docs/qa/QA_REVIEW_M4_PART7.md`. Changed: `engine/src/state/types.ts` + `save/saveGame.ts` + `state/createInitialState.ts` (the v10 rung + `economy`/`rooms` fields), `engine/src/actions/coreActions.ts` (the seam + loot gate), `engine/src/pipeline/applyAction.ts` (stage-4 spoilage), `engine/src/sim/{loot,inventory,survival}.ts` (economy loot gate, item weights, eatable fresh/spoiled food), `engine/src/combat/combat.ts` (durability wear on a strike), `engine/src/map/{types,regionGraph,seedWorld}.ts` (the `graph.recipes` pool), `engine/src/index.ts`, `harness/src/playCli.ts` (registers the pool). **Deferred:** base/stash spoilage + the community fridge/generator (T52, **PL-M4-29**); crafted outputs awaiting their effect systems — molotov/torch/warm-clothing + shelter barricade crafts (**PL-M4-30**); the economy dials (M5 balance, **PL-M4-31**); spoiled-food sickness cost + per-food eat choice (**PL-M4-32**); the repair-provenance word-band render + the legendary set (**PL-M4-33**); trading + hidden loot/NPC hints (FR-ECO-08/09, T53/T54, **PL-M4-34**). The 14-stage pipeline order is unchanged. See `docs/plans/M4_PART7_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART7.md`.

- **M4 — Part 6 (T50) gives the wider world a voice: the radio network — five signal families that *evolve with world state*, listening cheap and broadcasting a loud unknown-audience risk, one reserved anomaly.** **FR-STY-03** (GDD Part XIII · wireframe **SCR-09**). A new `engine/src/sim/radio.ts` interprets `content/radio/*.json` **generically** (the T47 encounter idiom — no per-signal branching) over a new transient **`graph.signals`** pool, gated on carrying a scavenged **`item.radio`** and inert otherwise. **The design call — signals are DERIVED, not stored:** `signalStatus` is a pure function of world + region + day, so the network **evolves for free** as the world already drifts — **no radio sim-layer, the 14-stage pipeline untouched, and NO save-schema rung (stays v9)**. **Emergency** loops die with the grid (`POWER_DEAD_AT`) or their shelf life; **military** evacs read **live → failing → dead** against their region's threat (`MILITARY_FAILING_AT 45` / `REGION_FALL_AT 65` — the already-overrun Downtown checkpoint is a **dead-air row from the start**, the Hillcrest evac holds); **civilian/ham** operators fall silent when overrun (`REGION_SILENT_AT 60`); the **unknown** number station is **faint by day, clear at night**. Signals **age** and a lost one **decays to a dead-air ROW that stays listed** — the silence is content (SCR-09). **Two append-only latches** ride the `radio.tuned` beats (no stored state): a **discovered** signal stays on the dial (so an onset signal can't blink with the day/night tide) and a station heard **DEAD stays dead** (a fallen station never un-falls even if its district later calms). **The seam** mirrors `infectionChoices`: **listen** (`LISTEN_COST 1h`, cheap) logs a beat and `radioLine` renders the on-air digest **FROM that beat**, so a within-turn world drift can't retroactively change what you heard; **broadcast** (`BROADCAST_COST 1h`) is **loud** — `params.noise = BROADCAST_NOISE 70` via the T14 override deposits region-scale noise at your node and the **T26 dead re-path to you** — with an honest **unknown audience** (one seeded draw on a lazily-seeded **`radio` RNG stream** picks a 'who hears?' hint, never a spawn; the loudness is stated in the choice label and again post-hoc, so 'nothing answers' never reads as free). **One reserved anomaly** breaks the row format **exactly once** (rendered **bare** — no channel, no pill, no age), gated to a **reachable-but-rare** deep-crisis: a deadly district (`≥ ANOMALY_THREAT 60`) or a blackout (`grid ≤ 25`), **at night, once per run**. **Content — a new schema-validated type**: `content/schemas/radio.schema.json` + **seven signals** across all five families + the anomaly (the schema gate goes **9 → 10 types**); `world.broadcasts` stays reserved (the derived model doesn't populate it). **Byte-identity of every prior run is preserved** — the system is inert without a pool *and* without `item.radio`, and `item.radio` is appended to the loot tables **only when the radio system is active** (a `floor(f·len)` pick shifts on a table growth, so gating it on the pool keeps radio-less runs drawing exactly as before). An **adversarial two-subagent audit** (engineering + design) ran and **11 fixes were applied**, including **two HIGH**: the **loot byte-identity breach** (radio had been added to the *shared* tables, silently shifting loot draws in radio-less runs — reverted and gated on the pool) and the **anomaly-reveal-eaten-by-within-turn-drift** (the reveal was re-derived at render time; now rendered from the logged beat, proven drift-proof end-to-end); plus **thresholds retuned to the sim's reachable band** + a **reachable anomaly gate** (the design lens proved `ANOMALY_THREAT 85` was unreachable — region threat mostly decays today), **broadcast loudness made legible pre-commit**, **digest label de-duplication**, a **hardcoded day** removed from a dead line, the **anomaly bare render**, and `strengthOf`/`ITEM_WEIGHTS` cleanups. CI green in a clean sandbox: **engine 471 (+22) · content-loader 9 · harness 93 (+8) · typecheck clean ×3 · schema gate 135 entries / 10 types · malformed rejected · empty-turn smoke exit 0.** New: `engine/src/sim/radio.ts`, `engine/test/radio.test.ts` (status derivation per family as the world moves, reach + faint-from-a-neighbour, the two latches, the drift-proof anomaly reveal, determinism + stream isolation, save-lossless, no-number legibility), `content/schemas/radio.schema.json`, seven `content/radio/*.json`, `harness/test/radio.test.ts` (content interpretation, the legibility gate, a shipped-content play beat — a station reaching dead air and the anomaly both proven **reachable** from shipped baselines, not hand-forced), `docs/plans/M4_PART6_PLAN.md`, `docs/qa/QA_REVIEW_M4_PART6.md`. Changed: `engine/src/actions/coreActions.ts` (the seam wiring), `engine/src/sim/{loot,inventory}.ts` (pool-gated radio loot + weight), `engine/src/map/{types,regionGraph,seedWorld}.ts` (the `graph.signals` pool), `engine/src/index.ts`, `harness/src/playCli.ts` (registers the pool). **Deferred:** the rumor system (FR-STY-05, **PL-M4-24**); a real broadcast *response* + the radio-room reach/broadcast (FR-STY-04 / T52 / T53, **PL-M4-25**); the live 'a district actively falls' dynamic, which needs the deferred horde→threat coupling (**PL-M4-26**, PL-M2-05); first-pass dials + the signal-set voice/casting pass (**PL-M4-27**); radio depth surfaces + the T54 codex + latch-scan perf (**PL-M4-28**). No save-schema rung; the 14-stage pipeline order is unchanged. See `docs/plans/M4_PART6_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART6.md`.
- **M4 — Part 5 (T49) turns infection from a death timer into an identity: a hidden, staged sickness that is *a harder way to keep playing*, never an instant Game Over.** The M1 loop (T22) modelled infection as exactly what GDD Part VI forbids — a **countdown to an instant loss** (a bite drives a hidden `progression`; at 100 the run just ends). Part 5 promotes it into the **staged identity** the design demands (**FR-INJ-05/06/07/08**), in a new `engine/src/sim/infection.ts` that holds the authoritative dials while `survival.ts` delegates the driver and the run-end. **Staging (FR-INJ-05):** `none → incubating (the GDD's *asymptomatic*) → symptomatic → advanced → terminal`, read **only from symptoms** — the hidden `progression` is **never shown**. Only `advanced` is inserted (at progression 70); the T22 `symptomatic` (40) / `terminal` (100) thresholds are unmoved, so every prior `stageFor` test holds. **The crux — no instant Game Over (FR-INJ-08):** reaching **terminal no longer ends the run** — it *opens* the **cure race**; the run ends by infection **only at a delayed `succumb`** (`progression ≥ 148`, ~24 in-game hours of continued *untreated* driver past terminal onset), so "no instant Game Over" is literal **and** the GDD's "some runs still end in death by infection" is preserved — the *authored, heightened* infection death scene is the clean seam left for **T62** (M5). Stop the driver (close the bite) or cure, and you live on terminal — the requirement made real. Progression now runs 0–148 on its own `clampInfection`. **Perception (FR-INJ-06):** at advanced/terminal `sceneOf` grows unreliable — **hallucinated leads** and, at terminal, **memory gaps** — chosen by a **stateless hash** (new `rng/prng.ts` `hashUnit` over the existing `cyrb128`) so a *render* stays pure, **advances no RNG stream**, and is **resume-safe**; every line self-negates ("Was any of it real?"), it is **suppressed whenever a real danger lead is on the board** (a hallucination can never sit beside — and undermine — a genuine horde read), and `availableActions` keys off real state so it can **never fabricate a threat**. The honest **"you can no longer trust your senses / tell what is real"** warning rides the always-shown status region at **advanced *and* terminal**, so distrusting the story channel stays legible and fair. NPCs react to the **visible sign**, and a **visibly-dying (advanced+) player can no longer recruit** — the FR-INJ-06 "dialogue closes" dimension, in `encounters.ts`. **Stage-scaled fever fatigue** (symptomatic +1 / advanced +2 / terminal +3 per hour) makes the run materially harder and **compounds the succumb clock** (more rest time = more driver). **Diagnosis / cure / quarantine (FR-INJ-07):** a contained `infectionChoices` seam (mirroring `shelterChoices`) offers **diagnose** (names the stage precisely via a `player.flags` flag — the opt-in clarity valve the tripwire mitigation calls for), **treat-infection** (antibiotics — **strong & certain early**, and **stage-scaled + *seeded-uncertain* at advanced/terminal** via a new lazily-seeded **`infection` RNG stream**: "costlier and less certain the deeper it goes"), and **quarantine** (shelter clean-conditions — strong early, weak at advanced, **not offered at terminal** so there is no dead-option trap); **each emits an honest, no-number outcome** (*cleared / eased / held*, logged as an `infection.treated` Living-History beat). All three verbs gate on **symptomatic+** — offering them while asymptomatic would leak the hidden clock, so the asymptomatic lever is the ordinary bite `treat` (close the wound, stop the driver before it ever shows). **Content — a new schema-validated type** `content/infections/` + `content/schemas/infection.schema.json` (**the schema gate goes 8 → 9 types**): `infection.bite.json` mirrors the engine's staged prose + cure/quarantine dials, **guarded byte-for-byte by a harness drift test** (the T40 arc pattern). **One save-schema rung — `SAVE_SCHEMA_VERSION 8 → 9`** (`migrateV8toV9`): the stage *bands* changed, so a v8 mid-infection save at progression 85 stored `"symptomatic"` (old bands) is **re-derived to `"advanced"`** on load — a pure, total value normalization (not a reshape) for the player and every actor; a healthy save is untouched. **The mystery/comprehension guarantee** (retiring the PRD §10 "infection-as-identity is confusing" tripwire) is proven by a strengthened harness **comprehension gate**: the four reads are **distinct and escalating**, the hidden number **never leaks** in status, scene, *or* the diagnosis line, and a cure at symptomatic **drops the stage and removes the fever line** so "the cure pulls you back" is felt — no decision ever needs the number. An **adversarial two-subagent audit** (an engineering lens + a design/comprehension lens) verified determinism, save-losslessness, and FR fidelity, and its findings were applied: the **quarantine-at-terminal dead-option trap** removed; **`advanced` added to the director's distress predicate** (it had been skipped, dipping difficulty at the middle stage); **quarantine now earns the shelter rest bonus** (it had silently forfeited it); the **hallucination suppressed beside a real threat**; the **terminal read carries the perception-warning**; **cure-outcome feedback** added; and the comprehension gate hardened. CI green in a clean sandbox: **engine 449 (+31) · content-loader 9 · harness 85 (+10) · typecheck clean ×3 · schema gate 128 entries / 9 types · malformed rejected · empty-turn smoke exit 0.** New: `engine/src/sim/infection.ts`, `engine/test/infection.test.ts` (staging, the no-instant-death + delayed succumb, cure determinism + stream isolation, perception statelessness, save-lossless, the anti-leak-while-asymptomatic property), `engine/test/infectionMigration.test.ts` (the v8→v9 rung), `content/schemas/infection.schema.json`, `content/infections/infection.bite.json`, `harness/test/infection.test.ts` (the comprehension gate + the content drift-guard + a shipped-content play beat). Changed: `engine/src/sim/{survival,history,encounters,shelter,director}.ts`, `engine/src/actions/coreActions.ts`, `engine/src/rng/prng.ts`, `engine/src/state/types.ts`, `engine/src/save/saveGame.ts`, `engine/src/index.ts`, `engine/test/survival.test.ts` (the two terminal-death assertions inverted to prove FR-INJ-08), `harness/src/play.ts`. **Retires the "infection-as-identity is confusing" tripwire.** **Deferred:** the authored terminal death scene (T62, **PL-M4-20**); non-bite infection sources (**PL-M4-21**, with the T51 economy); first-pass dial + diagnose-cost tuning (**PL-M4-22**, M5 balance); the Mind model / companion-infection / scars (**PL-M4-23**). The 14-stage pipeline order is unchanged; every prior **bite-free golden stays byte-identical** (all infection paths are inert at stage `none`). See `docs/plans/M4_PART5_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART5.md`.

- **M4 — Part 4 (T48) makes the world stop repeating itself: tagged-pool weighting + cooldowns, and a *measured* §4 repeat-rate gate.** Part 3 built the encounter interpreter and selected **by fit, deterministically** — no weighting, no cooldowns (the code carried "T48 replaces this" notes; `cooldownHours` was reserved-but-unenforced). Part 4 closes that, delivering **FR-ENC-01/02** (tagged pool + weighting + cooldowns) and **FR-CNT-04** (Rule of Three), and turning the **PRD §4 "verbatim encounter repetition < 5% within a full run"** target into a **hard, measured gate**. Selection becomes **tiered**: an eligible **one-shot** (scripted) beat still wins **deterministically by fit with no RNG draw** — a chain payoff or evolution beat is never randomized and never crowded out — so **every prior golden stays byte-identical**; only when the eligible set is **all repeatable** does the ambient model run (a single candidate returns **without drawing**; ≥2 draw one weighted pick from a new named **`encounter` RNG stream**, weighted to favour **fresh** — long since this id fired — and **tag-diverse** — long since anything sharing a tag fired — content). The elegant part: **the whole anti-repeat store rides the Living History.** Cooldowns and the §4 metric both read the `encounter.begin` beats T47 already logs, so **T48 adds no save-schema rung** (stays **v8**) and the `encounter` stream rides the existing open `rng.streams` map (lazily seeded, independent — it can't shift another system's sequence); the per-turn history look-back is **bounded** to the schema's 336h cooldown cap. Content gains two fields — **`tags`** (FR-ENC-01, the recombination/diversity handle) and **`weight`** — on `EncounterDef` + `encounter.schema.json`, and **`cooldownHours` is now enforced**. **Instrumentation** lands as `engine/src/telemetry/repetition.ts`, a pure, client-driven, off-by-default fold (mirroring the T32 pacing baseline) that turns the fires into **`verbatimRepeatRate`** — a same-id re-fire within a **48h recency window**, which cooldowns ≥ the window drive toward zero — plus `distinct`/`immediateRepeats`/`maxSingleShare`/`byCategory`. **The owner chose a hard CI gate over a reported-only signal:** `harness/test/repetition.test.ts` drives a deterministic **full-run city sweep** and asserts `fires ≥ 40`, `verbatimRepeatRate < 5%`, `immediateRepeats == 0`, `distinct ≥ 12`, `maxSingleShare ≤ 25%` — **measured: 134 fires across 54 days ⇒ 0.00% verbatim, 0 immediate, 20 distinct, 12.7% max share, across five categories** (the sweep holds nodes uncontested and claims no safehouse, so the combat/shelter classes are out of its scope by construction). A small **cadence rule** was added to `applyAction` (a new encounter never opens on a turn spent resolving one, so the denser pool can't stack beats onto a just-finished scene; inert for T47's sparse pool). **Content — a ~12-encounter demonstrator ambient pool** (owner-chosen over a launch-scale pour), tagged, repeatable, cooldown-spaced (all ≥ 72h > the window), across six of the seven categories (no ambient **combat** beat — encounters fire only on uncontested nodes per PL-M4-14, so the class stays the one-shot `overpass-toll`'s) in clusters that make **recombination** legible: **city-voices** ×3 (a noticeboard, a sprayed arrow, a child's schoolbag), **the-dead** ×2 (an uncovered body, a note in a pocket), **living-thing** ×2 (a stray dog, birdsong — living animals, human-only-undead canon held), **scavenger-sign** ×2 (chalk marks, a rival minutes ahead), a **passing-stranger** social beat, a **holding-the-base** shelter beat, and a **night-nerves** psychological beat. **FR-CNT-04** was completed by giving the two 2-choice authored anchors (`a-name-on-the-wall`, `figure-in-the-window`) a genuine third approach and confirming the shelter class offers ≥3. An **adversarial content-quality subagent audit** ran the Golden Encounter Formula over the new set → **fixes applied**: **3 HIGH** (two cost-free "obvious best" branches restored to real trades, and a dominated option whose label promised safety its effects never delivered given that safety), **5 MED** (a weak/overpriced trade fixed, and three near-duplicate skeletons re-flavoured/re-valued to break the clone feel — the task's own purpose), **LOW** cadence tics and stock phrases. CI green in a clean sandbox: **engine 418 (+16) · content-loader 9 · harness 75 (+10) · typecheck clean ×3 · schema gate 127 entries / 8 types · malformed rejected · empty-turn smoke exit 0.** New: `engine/src/telemetry/repetition.ts`, `engine/test/encounterSelection.test.ts` (cooldown-from-history, single-candidate no-draw byte-identity, weighted determinism + stream independence, scripted-never-crowded-out, a 200-seed tag-diversity distribution), `engine/test/repetition.test.ts` (the §4 metric math incl. a repeat-DETECTION case so a green gate isn't vacuous), `harness/test/repetition.test.ts` (the hard gate + Rule of Three + a tags/cooldown drift guard), 12 `content/encounters/`. Changed: `engine/src/sim/events.ts` (the tiered weighted/cooldown selection), `engine/src/pipeline/applyAction.ts` (the cadence rule), `engine/src/index.ts`, `content/schemas/encounter.schema.json`, and the two anchor encounters. **Retires the "Content-hunger repetition" tripwire's measurement gap** — the rate is now gated, not guessed. **Deferred:** the deep launch pool (extends PL-M4-16), a fire-probability cadence lever (**PL-M4-18**), narration-variant recombination (**PL-M4-19**); Humanity's consumers still land with T53/T61/T62 (PL-M4-15). No save-schema rung; the 14-stage pipeline order is unchanged. See `docs/plans/M4_PART4_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART4.md`.

- **M4 — Part 3 (T47) makes the simulation *speak in scenes*: a data-driven encounter system, and the first save-schema rung since v7.** Part 1 poured the city, Part 2 filled it with people and dead; Part 3 builds the layer that decides **which slice of the sim becomes this turn's scene** (GDD Part VIII). The hard requirement is **FR-CNT-03 — requirements + effects declared in *data*, no hard-coded branching** — so the engine ships an **interpreter**, not a switch of encounters: a new `sim/events.ts` reads authored `content/encounters/*.json` (validated by a new `encounter.schema.json`), threaded into the pure engine as **transient content on the graph** exactly as the node graph is (`buildRegionGraph`/`startRun` gain an opt-in pool; empty ⇒ inert ⇒ every prior golden byte-identical). A closed vocabulary of **requirement predicates** (node/region/kind/phase, node-state bands, humanity/mind, flags, shelter, a carried item, an npc present, a stash minimum) and **effect verbs** (`setFlag`/`setRegionFlag`/`adjustHumanity`/`adjustTrust`/`adjustNeed`/`adjustMind`/`grantItem`/`takeItem`/`depleteStash`/`inflictWound`/`seedWalkers`/`addNoise`/`revealDiscovery`/`logHistory`/`advanceStage`/`endEncounter`/`scheduleFollowup`) is applied purely by the interpreter. All six FR-ENC sub-requirements land: **categories** (05, all seven — exploration/combat/social/environmental/story/psychological/shelter), **chains** (03 — a `setFlag` fact a later `requiresFlags` reads, plus a *timed* `scheduleFollowup` via the existing `queue`, resolved stage 12), **multi-stage** (04 — an `advanceStage` effect moves the active encounter, persisted in the reserved `player.quests` slot like a fight persists in `combat`; a `seedWalkers` effect hands a stage off to a real T15 fight — negotiation → fight → chase), **moral → Humanity** (06), **false encounters** (07 — tension, no payoff), and **evolution** (08 — three encounters sharing a node id gate on `searchPct`/`walkers`/flags, so the same place yields before/during/after variants). Selection is **by fit, deterministic** (most-specific-then-id, no RNG this part — a run still replays byte-for-byte; weighting/cooldowns are T48), fires only on **uncontested** nodes (a walker node is the T15 prompt), captures the turn while engaged, and **always offers a way out** (an engine "Step away" when every authored choice is gated — no soft-lock). **The Humanity system arrives — owner-chosen as a stored scalar, not a derived read:** a hidden `player.humanity` 0–100 (baseline 50, *never a bar* — surfaced only as felt prose at the extremes), moved by moral choices and logged as a `moral` Living-History beat the M5 endings (T61/T62) will read. It ships as the **first save-schema rung since v7 — `SAVE_SCHEMA_VERSION 7→8`** with one forward-only total `migrateV7toV8` (seeds `humanity:50` on every historical save — a pre-moral run reads neutral); the encounter machinery itself adds **no** further rung (it rides the reserved `player.quests`/`player.flags`/`queue`/`region.storyFlags`). **Content — a ~14-encounter proof set** (owner-chosen over a larger pour), homed on shipped nodes, exercising every mechanic: the `garden-center` before/during/after **evolution triple**, the `overpass-toll` **multi-stage** negotiate→fight→chase, two **moral** beats (the marina cabin, the canal scavenger — a real cost on every branch), two **false** beats (the sound at the door, the figure in the window), a **timed chain** (a name scratched on a wall → a day later, the one who scratched it, where *burying the dead preserves Humanity*), a **shelter** watch, a **psychological** long-dark, a first-light opener, and the depleting-world `stripped-bare` beat — across all seven categories. The playable client (`playCli`) registers the pool; the golden-transcript generators deliberately don't, staying byte-stable. An **adversarial content-quality subagent audit** ran the Golden Encounter Formula over the set (human-only canon + non-audio clean) → **11 fixes** applied (five strictly-dominant "obvious best" choices given a real time/resource trade, three moral branches given a cost on every axis, a mislabeled "share" that granted no food, a rescue given a run-memory trace, two cadence tics) — which also motivated a small requirement addition, `minStash`, gating "pay the toll" on actually holding a can. CI green in a clean sandbox: **engine 402 (+30) · content-loader 9 · harness 65 (+8) · typecheck clean ×3 · schema gate 115 entries / 8 types · malformed rejected · empty-turn smoke exit 0.** New: `engine/src/sim/events.ts`, `engine/test/events.test.ts` (requirement matching, every effect verb, selection determinism, multi-stage, chains, evolution, false, moral clamp, the seedWalkers→combat handoff, save-lossless with an active encounter, anti-softlock), `engine/test/humanityMigration.test.ts` (the v7→v8 rung), `content/schemas/encounter.schema.json`, 14 `content/encounters/`, and `harness/test/events.test.ts` (category coverage + a content drift-guard + a golden play over shipped content). **Deferred to T48** (as the roadmap sequences): FR-ENC-01/02 — the tagged-pool **weighting**, **cooldown** suppression, and the §4 **verbatim-repeat-rate** gate; Humanity's *consumers* (ending gates T61/T62, companion desertion T53) read the scalar later. The 14-stage pipeline order is unchanged. See `docs/plans/M4_PART3_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART3.md`.

- **M4 — Part 2 (T45–T46) fills the city with its people and its dead — and, for the first time in M4, changes the engine to make both play distinctly.** Part 1 poured the map; Part 2 puts teeth on it. On all three scope forks the owner chose the fuller option, and every one shipped **without a save-schema rung** (`SAVE_SCHEMA_VERSION` stays **7**). **T45 — the survivor pool + companions (FR-NPC-01/03/04).** The handcrafted pool grows **3 → 18**: **15 new named survivors**, three per non-Rivermouth district, each with a `disposition`, a `homeNode`, a one-line `background`/`personality`, and a real withheld `secret` in the Sarah/Marcus/Ruth voice — including the GDD-named **Dana** ("won't say what she did before") — with dispositions across all five temperaments. The pool is homed **entirely outside Rivermouth** (so the fun-gate slice population is unchanged) and every survivor is `homeNode`-pinned (so `spawnNpcs` draws **zero** from the `npc` RNG stream — the sequence is untouched). Companions get the **full FR-NPC-03**: a **bounded party** (`PARTY_CAP=3`, a fourth recruit refused), **recruit eligibility** (a `hostile` survivor never joins, however high trust runs — closes QA L3), **named companions** (an optional `name`+`trust` carried onto the `Survivor` at recruit — no rung — so party prose reads "Ana is with you" not "your companion", closes QA L1), and **trust-gated standing orders** — **follow**/**hold** always, **scavenge**/**guard** gated on `trust ≥ ORDER_TRUST_MIN (80)` and *earned* by feeding (`COMPANION_SHARE_TRUST`), so a fresh recruit at 70 must be earned up before you can send them into harm's way. Orders ride `order:*` **boolean flags** (no schema change); `tickCompanions` respects them — **scavenge** banks `item.canned-food` into the base stash every `SCAVENGE_HOURS_PER_UNIT` hours (drains faster for the exposure — closes part of **PL-M3-01**, the base becomes a loop not only a pack drain), **guard** maintains the node's barricades against the T38 decay. **T46 — the full zombie roster (FR-CBT-06/07 · FR-AUD-06).** The GDD §IX roster is completed with the four **combat-distinct** types: **Fresh** (`swift` — a presence surge drives it to *chasing* where a plain node only *investigates*; `initiative` — answers **every** melee exchange), **Crawler** (`lowProfile` — reads calmer than it is while you're only near it, full the instant you stand on it; `graspWound` — a higher catch chance on a slip and an **ankle sprain** not a random blow), **Bloated** (`burstInfection` — putting it down at your node, by blow **or** shot, sprays a bite-severity infectious wound into the T22 track, so you learn to **slip past**), and **Riot** (`armor` — blunt strikes skid off to ~0 net, a **firearm pierces** — a bullet or a wide berth; *hard, never a softlock*, since a slip is always offered and a firearm two-shots). It ships as two schema extensions (`zombie`: `signature`/`swift`/`lowProfile`; `enemy`: `armor`/`burstInfection`/`graspWound`/`initiative`), the `zombies.ts` state machine (a 7-type behaviour table + `swift`/`lowProfile` in `stimulusAt`), and a **type-aware `combat.ts`**: an authoritative `ENEMIES` dial table (content-mirrored, a harness test guards drift), `enemyForNode` picking the **most dangerous combat-distinct type present** (`riot > bloated > fresh > crawler`, else a walker — so **screamer/stalker fight as walkers**, the seam that keeps every pre-T46 encounter **byte-identical**), with armor/burst/grasp/initiative wired into resolution and the type + its signature named in the narration. **No save-schema rung** — every combat difference derives from the `CombatState.enemy` id we've stored since T15, so a walker fight is byte-identical and the whole **349-test engine baseline stayed green untouched**. **Human-only canon** is held on all seven types, and **FR-AUD-06** is satisfied — every type ships a **non-audio** signature (a visual/readable tell), harness-guarded, so the roster reads with sound off. The city was **re-seeded** (26 nodes across the 5 non-Rivermouth districts) so each district's menace matches its identity — Riot at the fallen civic/police lines, Bloated through the hospital and the waterworks, Fresh where crowds turned fast, Crawler in the homes and machine floors — and every combat-distinct node was given `walkers > 0` so it is a **live threat, not an inert tag** (closes **PL-M2-02** / **PL-M4-02** for the seeded nodes); Rivermouth is left unchanged (its marina stalker stays intentionally inert — PL-M4-10). An **adversarial content-quality subagent audit** ran over the new characters + monsters (verdict *ship-with-fixes*: canon + FR-AUD-06 clean, dark secrets restrained); **11 fixes** were applied (5 description rewrites to break a reused template, 4 secret rewrites to differentiate overlapping "keeper-of-the-dead" beats and lift two below-bar secrets, 2 zombie polish). CI green in a clean sandbox: **engine 372 (+23) · content-loader 9 · harness 57 (+9) · typecheck clean ×3 · schema gate 101 entries / 7 types · malformed rejected · empty-turn smoke exit 0.** New tests: `engine/test/roster.test.ts` (type-aware combat + `swift`/`lowProfile`), `engine/test/orders.test.ts` (party cap, eligibility, orders, scavenge/guard, trust gate, feeding, save-lossless), and a roster/pool drift-guard in `harness/test/content.test.ts`. Retires **FR-CBT-07** at full-combat depth and **FR-NPC-03** at full depth; the human casting/voice pass stays the owner's beta gate (PL-M4-12). `SAVE_SCHEMA_VERSION` unchanged at 7; the 14-stage pipeline order unchanged. See `docs/plans/M4_PART2_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART2.md`.

- **M4 — Content-complete city is UNDERWAY; Part 1 (T43–T44) resolves the M4 decisions and pours the full first city.** The foundation before the content pour, and no engine code with it. **T43 — three §9 decision gates resolved as ADRs** so none stalls content or the beta. **ADR-0004 (platform ordering)** ratifies **web-first** as the sole launch surface through beta + v1.0 and formally **defers the native/Steam/bot ordering** to the v1.0 launch gate (T69) with a named trigger (→ ADR-0004a) — decided against beta telemetry, not a guess. **ADR-0005 (cross-run memory, PRD Open Q6)** adopts a **bounded, local, per-profile Chronicle** (endings + fallen/surviving survivors + a rolling window of Living-History beats, its own version + forward-only ladder) plus a **capped set of light unlocks** (starting-kit variants, seen recipes, cosmetic titles) chosen so none grants a mechanical head-start — the deterministic source M5 endings (T61/T62) read. **ADR-0006 (licensing, PRD Open Q8)** closes the gate on the only question the beta actually asks — the M4 beta ships **all-rights-reserved**, the placeholder `LICENSE` standing as its real license (explicit, not an open question), so distribution is unblocked — and formally **defers the final selection** (open source / source-available / proprietary) to the **coupled T58 monetization decision** with a named trigger (→ **ADR-0006a**), mirroring how ADR-0004 defers the platform ordering to T69. The reasoning is an ordering one: permissive licensing is a **one-way door** (shipped copies cannot be recalled) while all-rights-reserved is not, so the **irreversible call is made after the business model it constrains, not before it** — T58 inherits a full option space instead of one foreclosed by side effect, and the real arguments for opening (the dependency-free core + content-as-data with open schemas make the project unusually moddable and portfolio-facing) are recorded in the ADR to be weighed there, not lost. `LICENSE` is unchanged; the decisions register (`design/decisions/README.md`) becomes a log of 0001–0006. **T44 — the full first city (FR-MAP-01 / FR-SIM-02)** replaces the single Rivermouth slice with **6 regions and 60 nodes** (top of the §6.4 ≈40–60 band): **Rivermouth** (start, the M1 ring expanded 6→8), **Downtown Core** (dense/loud/high-tier), **The Terraces** (suburbs where the living hide), **Mercy Hospital** (the medical mother lode, extreme danger), **The Ironworks** (materials/fuel/tools), and **Hillcrest** (university + reservoir + radio, the high ground) — **one connected graph** from the single start (`node.rivermouth.transit-plaza`) via **9 inter-region gateway routes**, region adjacency wired, **14 claimable safehouses**, walker/screamer/stalker seeding (full roster is T46), first-pass per-district identity baselines. Every node is **content-as-data** (FR-CNT-01): five-question prose, a location `kind` (the FR-ECO-02 loot table), symmetric adjacency, per-run memory the engine seeds at run start (searchPct/damage/corpses/noise/fog/… — persist all run). Authored via a **validated generator** (asymmetry / dangling-edge / disconnection impossible at author time), then verified by the **real engine `buildRegionGraph` + `startRun`** and the schema gate; an **adversarial content-quality subagent audit** surfaced 5 fixes (ambulance-bay `kind` police→generic; a 'zombie dog' reframed to keep the human-only undead canon; three motif de-duplications). **Rivermouth's six shipped nodes keep their exact prose/kind/flags** (only city adjacency added) and the region keeps its `loadContent`-pinned baseline (threat 35, loot 70), so the M3 arc and every pin hold. **No engine code, no schema change, no RNG, no save-schema rung** — the only code change is `harness/test/content.test.ts`'s node-count cap, updated from the M1–M3 slice budget (5–8) to the **M4 city budget (40–65, 6 regions)** and strengthened into a T44 guard (every region populated, cross-region routes exist, ≥5 safehouses). No engine test reads shipped content, so the whole engine suite is untouched; the bigger world shifts Rivermouth's region-drift RNG from turn 2 (shared `region` stream) but every behavioral harness test holds. CI green in a clean sandbox: **engine 349 · content-loader 9 · harness 48 · typecheck clean ×3 · schema gate 7 types / 78 entries · malformed rejected · empty-turn smoke exit 0.** The **§6.4 node cap is now hit (60/≈60)** — the PRD's signal to stop authoring breadth and go playtest before more M4 pours. Retires the M4 §9 decision gates (ADR-0004/0005/0006) and lands FR-MAP-01 / FR-SIM-02 at city scale. See `docs/plans/M4_PART1_PLAN.md` and `docs/qa/QA_REVIEW_M4_PART1.md`.

- **M3 is COMPLETE — the Slice Fun Gate (T42) PASSED; M4 opens and the content schemas freeze for authoring.** The owner playtested the finished vertical slice across three sessions and signed the decisive human gate (PRODUCTION §4, Stage 2): **a run becomes a story.** The authored arc *The Last Customer* delivered end-to-end from systems that were not scripting the player — the hours spent searching, claiming, fortifying and stashing a base are hours Ruth goes without, so she arrives desperate at the barricade on her own; a now-legible costed choice spends the cache you banked to take her in; and ~12h later the arc closes as she repays your store (*'Ruth slipped back at first light and left supplies in your cache — she remembered'*). Session 1 caught a genuine legibility defect — the *Take Ruth in* option was silently gated on the cache holding ≥2 units — which was fixed by **surfacing the requirement in words** (the plea now states the cost and the shortfall, the option carries its cost, and a foreshadow nudges you to keep supplies stashed) and confirmed across Sessions 2–3; the gate rule was honoured (re-invest in the slice, don't pour content). The pass **retires the 'systemic but not fun' risk**, **opens M4 (content-complete city)**, and **freezes the content schemas**. Carried into M4/M5 as the scope the pass unlocks (not blockers): a scarcity/`stashDraw` tune so the choice's resource cost bites, a *played* base siege on the good branch (the narrated horde never arrives — PL-M2-02 / PL-M3-08), more arcs for across-run variety (PL-M3-09), and a repay-prose/clock polish. An interactive slice CLI shipped for the gate (`npm run play:slice`, `harness/src/playSlice.ts`). Engine **349** + content-loader 9 + harness 47 green; `SAVE_SCHEMA_VERSION` 7; schema gate 7 types / 19 entries. See `docs/qa/FUN_GATE_LOG.md` (Sessions 1–3).**

- **M3 — Part 4 (T39–T41) closes the M3 build: a shared stash, the first authored story, and a run that reads as a story — the Slice Fun Gate (T42) is staged for the owner's verdict.** The four M3 promises stand in one frame. **Shared stash (T39, `sim/stash.ts`)** gives the base a warehouse: **deposit/withdraw** loot at your shelter into `Player.stash` — a store the T18 carry budget does not see, so a run banks surplus while **weight still bites on the road** — both **free** (0h, like the T18 drop) and offered only while standing in your own base; `depleteStash` is the contested-world **raid hook** (peels units in stable order, logs `stash.raided`). This block's **one additive schema rung** — `Player.stash`, `SAVE_SCHEMA_VERSION 6→7` with a forward-only idempotent `migrateV6toV7` (appends `stash:[]`) — empty and inert on every prior run. **First authored story arc (T40, `sim/story.ts`)** ships *The Last Customer*: Ruth, the desperate shopkeeper, turns up at the base you fortified (the plea fires from the sim — the hours you spend making a home are hours she goes without), a **costed help/refuse fork**, and a **delayed consequence that ripples** — take her in (spend the cache) → trust and she repays; turn her away → trust collapses below parley and she comes back to **raid your stash and knock the barricade** (the raided-stash story beat). A **deterministic trigger chain over the sim** with **no rung** — it rides the reserved-and-inert `story.progress` (beat counter) + `queue` (delayed consequence); two identity pipeline stages **graduate** (12 `resolveQueue`, 13 `evaluateStory`) with names + 14-stage order unchanged, inert when no arc is active. Arcs are **opt-in** (`registerArcs` via `startRun`), so every prior golden is byte-identical. New authored `content/arcs/` + `content/schemas/arc.schema.json` (schema gate now **7 types / 19 entries**); the engine holds the VS dials and a harness integrity test guards content↔engine drift. **Story surfaced in the Scene (T41)** composes a `storyLine` into `sceneOf` alongside the world/people/shelter leads and offers the arc's choices via `availableActions` — plain-text, keyboard-only, rendered by the existing client with no rewrite, so **a run reads as a story** (see `docs/qa/FUN_GATE_SLICE.md`, an end-to-end two-branch playthrough). No new RNG, integer-only, save-lossless (a stashed, mid-arc run round-trips deep-equal). An adversarial subagent review found and I fixed one legibility bug (the arc's payoff line dropped when world events trailed the stage-12 beat — now scans for the beat at `meta.turn`, regression-tested). Engine **348 (+26)** + content-loader 9 + harness **47 (+5)** green; typecheck clean across all three packages; schema gate green (7 types / 19 entries) and malformed still rejected; empty-turn smoke green; the 14-stage order unchanged. Retires FR-SHL-03 / FR-PLR-04 / FR-STORY-01 / FR-UI-STORY (VS subsets). **T42 (Slice Fun Gate) is prepped, not passed** — the owner's human 'a run becomes a story' verdict (criteria, verdict template, and a candid provisional read in `docs/qa/FUN_GATE_LOG.md`); **M3 stays active until it is signed.** See `docs/plans/M3_PART4_PLAN.md` and `docs/qa/QA_REVIEW_M3_PART4.md`.

- **M3 — Part 3 (T37–T38) gives a run its first place that is yours: claim a base, fortify it, keep it up.** The map stops being uniformly hostile. **Claim a shelter (T37)** lets the player plant a flag on a node they have **searched clean** (`searchPct >= 100`) while holding no shelter yet — one active base per run — setting the reserved `Player.shelterId` and logging `shelter.claimed`; resting at the base recovers extra fatigue (**deeper rest**). **Fortify & upkeep (T38, `sim/shelter.ts`)** spends **one `item.scrap`** (already in the T17 loot tables) + time to raise the reserved `NodeState.barricades` by 25 toward a cap of 100; fortification **decays** by 1/hour in pipeline **stage 6** and must be topped up — a live maintenance decision, not a one-time buy. A fortified base earns three integer-scaled payoffs (`trunc(max·barricades/100)`): it **muffles its own noise** (so hordes are less drawn — one mechanism delivers both *dampen node noise* and *resist horde drift*), **raises a detection floor** in `stimulusAt` (dormant by day under the player's presence at full fortification; a stalker at night *reduced, never nullified*), and **rests you deeper**. A new `shelterLine` surfaces the base in the Scene (plain text, keyboard-only), and the Living History learns `shelter.claimed` / `shelter.fortified`. **No save-schema rung this block** — both facts were reserved in the T3 shape and inert until now, so `SAVE_SCHEMA_VERSION` stays **6** and a claimed, fortified run round-trips losslessly with zero migration (the same *populate-a-reserved-shape* move T36 made for `actors`). No new RNG stream and both fields untouched in every prior run, so every M2/M3P1/M3P2 golden run stays **byte-identical**; a scripted search→claim→fortify→rest slice reproduced byte-for-byte, round-tripped losslessly, kept every numeric leaf an integer, and every resolved shelter turn moved a tracked system (FR-CORE-04 clean). Engine **322 (+26)** + content-loader 9 + harness 42 green; typecheck clean across all three packages; schema gate green (6 types / 18 entries, content unchanged) and malformed still rejected; empty-turn smoke green; the 14-stage order unchanged. Retires FR-SHL-01/02 (VS subsets). See `docs/plans/M3_PART3_PLAN.md` and `docs/qa/QA_REVIEW_M3_PART3.md`.

- **M3 — Part 2 (T35–T36) makes the people layer played: survivors are met, fed, threatened, recruited, and lost.**
  The silent substrate T33/T34 shipped now reaches the Scene and bites. **Survivor encounters (T35, `sim/encounters.ts`)**
  surface every living survivor at the player's node as choices — *talk* (a one-shot that flips a new `NPCState.met`,
  gated by `canParley`), *share food/water* (spend one of your items to buy their need down by the T22 relief and earn
  trust, `share +10`, offered only when carried and pressing), and *threaten* (`−20`) — wired into `availableActions`/
  `sceneOf` so the harness surfaces them in plain text with no client rewrite. The needs model finally has **teeth**:
  `driftNpc` kills a survivor whose hunger or thirst saturates (`NEED_FATAL`), so `alive` flips and the world has someone
  besides the player to fall on; threaten past `PARLEY_MIN` **turns** a survivor cold, and the no-regen property keeps that
  door shut. **Recruitable companions (T36, `sim/companions.ts`)** graduate a met survivor at `trust ≥ 70` out of `npcs`
  into an `actors` `Survivor` flagged a companion (`recruit`); pipeline **stage 5** now drifts companions and keeps them at
  the player's side (they follow), fed from your pack through the same share verbs; a neglected companion dies **permanently**
  — removed from `actors`, remembered by a `fallen.<id>` flag and a `companion.died` entry in the Living History
  (`killCompanion` exposes the same seam for a future combat death). The **Living History** learns four people events
  (`npc.met`, `npc.died`, `companion.recruited`, `companion.died`). One additive schema bump this block —
  `SAVE_SCHEMA_VERSION 5→6` with one forward-only rung `migrateV5toV6` (every old survivor gains `met:false`); `actors` is
  populated by runtime transition on an existing always-`{}` collection, so T36 rides no rung. No new RNG stream, so every
  M2/M3P1 golden run stays byte-identical; a scripted meet→recruit→travel slice reproduced byte-for-byte, round-tripped
  losslessly across v6, and kept the FR-CORE-04 audit clean. Engine **296 (+31)** + harness **42 (+3)** + content-loader 9
  green; typecheck clean; schema gate green (6 types / 18 entries, content unchanged) and malformed still rejected; the
  14-stage order unchanged and the reserved `groups` untouched. Retires FR-NPC-03/04 (VS subsets) and the FR-NPC-01
  surfacing. See `docs/plans/M3_PART2_PLAN.md` and `docs/qa/QA_REVIEW_M3_PART2.md`.

- **M3 — People, shelter & first story is UNDERWAY; Part 1 (T33–T34) lays the people substrate.** A run now has
  *people* in it. **Survivor NPCs (T33)** add a new per-run `GameState.npcs` of `NPCState` (name, disposition, needs,
  location, alive, trust) behind `SAVE_SCHEMA_VERSION 4→5` (one forward-only rung `migrateV4toV5`, seeds `npcs:{}`); a
  handcrafted Rivermouth pool — Sarah the paramedic, Marcus the ex-Guard, Ruth the shopkeeper — ships as `content/npcs`
  behind a schema-first `npc.schema.json` (the gate now covers 6 types / 18 entries). Survivors spawn **deterministically
  from a new named `npc` RNG stream** (`spawnNpcs` in `startRun`) and their needs grind on with the hours via the T22
  economy, wired into pipeline **stage 5** (`updateCompanions` body; the fixed 14-stage order is unchanged); `npcs` joins
  the FR-CORE-04 audit. **Trust & disposition (T34, `sim/trust.ts`)** gives each survivor a 0–100 trust scalar seeded from
  disposition, moved only by the player's actions — help/share/trade raise it, threaten/rob/abandon lower it, asymmetrically
  so a betrayal costs more than a good turn earns — that **never regenerates on its own** (a betrayal sticks), with tier
  bands and `canParley`/`canRecruit` gates staged for T35/T36. Engine-first: no client surfacing yet (per the plan). Over a
  40-turn Rivermouth golden run the pool spawned at its home nodes, reproduced byte-for-byte, round-tripped losslessly
  across the v5 bump, kept **0 no-op turns**, and its survivors were simulated (a paramedic's thirst climbing) and
  persisted. Engine 265 (+26) + harness 39 + content-loader 9 green; schema gate green incl. the new npcs and malformed
  still rejected; one schema bump this block. See `docs/plans/M3_PART1_PLAN.md` and `docs/qa/QA_REVIEW_M3_PART1.md`.

- **M2 — Reactive world is COMPLETE (T23–T32); the living world is now visible in play.** Part 2 filled the
  last two world-sim layers and made the whole reactive world perceivable. **Time-of-day danger (T28)** gives the
  phase one owner (`sim/timeOfDay.ts`): darkness conceals a stealth mover (the T15 detection term, now named), night
  searches carry farther (extra deposit through the T14 noise override), and a diurnal `globalThreat` tide rises after
  dark and ebbs by day. **Route conditions (T29)** give every edge a weather-driven `wear` (`GameState.routes`,
  `SAVE_SCHEMA_VERSION 3→4`) that reads as clear/costly/flooded/blocked and changes move cost and availability, rising
  fast under a storm and receding slowly. **The Apocalypse Director (T30)** is a bounded bang-bang pacing controller
  that nudges the current region's danger by a clamped ±1 — escalating a calm run (so an unwatched district *festers*,
  not calms), relieving an overwhelmed one — and **can never produce an impossible state**; disable it with a flag and
  the pacing metrics move while every dial stays legal. **Living History (T31)** records notable events (weather turns,
  nightfall, horde moves, route changes, cleared fights, run end) append-only into `GameState.history`. **Telemetry
  (T32, `telemetry/pacing.ts`)** captures the PRD §4 pacing/pressure proxies — off by default, deterministic — and
  proves the director's Definition of Done. A light **scene-surfacing pass** puts the weather, the danger tide, an
  approaching horde, a screamer's shriek, and each route's condition into the narration and header, so the M2 *"the
  slice feels alive"* gate can finally be judged (addresses the Part-1 QA finding H1). Over a seeded Rivermouth run the
  world reproduced byte-for-byte, round-tripped losslessly across the v4 bump, recorded its own history, and still
  moved a real system **every resolved turn (0 no-op turns)**. Engine 239 + harness 39 + content-loader 9 green; schema
  gate green over 5 content types; one schema bump this block. See `docs/qa/QA_REVIEW_M2_PART2.md`.

- **M2 — Reactive world is UNDERWAY; the six-layer world simulation is live and Part 1 (T23–T27) is done.**
  The world now moves whether or not the player acts. A new world-sim substrate (`worldSim.ts`) holds six
  independently-tickable layers — zombies, regions, weather, time-of-day, hordes, director — behind a uniform pure
  `tick(state, ctx)`, with an `advanceWorld(state, hours)` that advances the world *off-screen* with no action
  submitted; the fixed 14-stage pipeline delegates to the layers, its names/order unchanged. Riding that substrate:
  off-screen regional threat/density drift (T24), a senses-driven zombie state machine with the first distinct types
  Screamer + Stalker (T25), migrating hordes that re-path to a logged gunshot (T26), and weather that couples stealth,
  power, and roads (T27). Over a 50-turn Rivermouth run the region's threat drifted 35→6, a horde migrated across the
  district, the zombie machine cycled dormant→…→chasing→hibernating, and **every resolved turn still moved a real system
  (0 no-op turns)**. Engine 196 + harness 39 + content-loader 9 green; schema gate green over 5 content types.

- **M1 — Core loop playable is COMPLETE; the Loop-Feel Check (§4) PASSED (qualified), and M2 is open.**
  After the T22 survival pass gave the loop turn-to-turn stakes, the Stage-1 Loop-Feel Check was run over a live
  playthrough: the survival clock now makes move/search/rest a real trade (the pivot moment — a turn-7 "thirsty and
  tired" prompting a Drink-water decision). Verdict logged in the tracker: **pass (qualified)** — the loop grips
  enough that world reactivity (M2), not more loop tuning, is the right next investment. Two entry notes carried to
  M2: the opening ~day is still too calm (land pressure by ~turn 3), and decisions stay maintenance-level until
  scarcity bites (M2's contested world supplies the dilemmas). All 12 M1 tasks (T10–T22) done; engine 146 +
  harness 39 + content-loader 9 green; schema gate green.

- **M1 — Core loop playable was FEATURE-COMPLETE; the Loop-Feel Check (§4) was the only thing left.**
  All 12 M1 tasks (T10–T21) are built and green: a fog-of-war region graph with node memory, the
  move/search/rest loop with time cost, per-turn change telemetry, noise, named wounds, avoidable
  combat with loud firearms and a stealth path, a finite/contested/depleting loot economy, a
  weight-limited pack that forces a leave-behind, a story-first single-decision client, an
  accessibility baseline (keyboard-only, all-critical-info-in-text), and lossless quit/resume at any
  turn boundary. Engine 133 + harness 36 + content-loader 9 tests green; schema gate green over
  enemies + nodes + regions + wounds; the 100-turn telemetry audit clean. The one remaining M1 exit
  criterion is the **owner's Loop-Feel verdict** — *is picking an action interesting, turn to turn?*
  — which is a human gate and cannot be auto-passed (PRODUCTION §4). M2 stays closed until it passes.

- **M0 — Foundation is COMPLETE.** The deterministic skeleton runs: a fixed 14-stage turn
  pipeline, seeded named-stream RNG, a content loader + schema gate, versioned lossless
  save/load, and a terminal harness that resolves an empty turn. Definition of done met —
  `applyAction(state, action)` runs the full pipeline deterministically (same seed + state =>
  byte-identical result), a save round-trips losslessly, and CI rejects malformed content.

### Decided

- **ADR-0003 — Save storage & versioning is ACCEPTED (M1 · T10)**
  (`design/decisions/0003-save-storage-versioning.md`): saves are **local-first and
  client-owned** — the dependency-free engine keeps doing zero I/O and only emits/consumes the
  T7 `SaveFile` string, which each client persists locally (harness/native → an atomically
  written file under the OS app-data dir; web → **IndexedDB**, not `localStorage`). Persistence
  is a **single rolling autosave slot per run**, written write-temp-then-atomic-rename with a
  retained `.bak`, so no crash can corrupt more than the most recent turn (zero-corruption
  target, NFR-SAVE-02). A save is exportable/importable as text so it doubles as a bug repro
  (DESIGN §9). Migration is the **T7 forward-only ladder** — one integer `SAVE_SCHEMA_VERSION`
  bumped only on a breaking state-shape change, one pure/total `N → N+1` rung per bump,
  newer-than-build refused. Optional cloud sync is deferred post-launch as an additive transport
  over the same string. Unblocks T11+ to autosave real runs toward the M1 lossless quit/resume
  gate.

### Changed

- **Play CLI renders cleanly on Windows consoles (M1 · T19/T21 fix)** (`prototype/harness/src/playCli.ts`): the interactive loop now creates readline with `terminal: false` (readline never echoes or redraws the input line) and writes each screen as one atomic block via an async line iterator that drains buffered input and ends safely on EOF. Fixes a first-paint glitch where the opening frame could repeat the "What do you do?" prompt and drop the location text on some Windows terminals; game logic was never affected. Playtest feedback, 2026-07-05.
- **Drop options no longer clutter the single-decision screen (M1 · T18 refinement)** (`prototype/engine/src/sim/inventory.ts`, `src/actions/coreActions.ts`): drop choices are now surfaced **only when the pack is heavy** (`PACK_HEAVY` = 3/4 of capacity, 30/40) — below that there is ample room, so a per-stack "Drop" on every turn was noise that fought the story-first screen (FR-UI). Labels are also humanized (`itemName`: "item.canned-food" → "Drop canned food") instead of showing raw content ids. Playtest feedback, 2026-07-05. Engine 134 + harness 36 green.

### Added

- **Six independently-tickable simulation layers — the M2 substrate (M2 · T23 · FR-SIM-01)** (`prototype/engine/src/sim/worldSim.ts`): FR-SIM-01's six state layers already existed in `GameState`; what M1 lacked was the ability to advance the *world* side on its own clock. T23 adds a first-class **world simulation** of six named, pure, independently-tickable layers (`zombies, regions, weather, timeOfDay, hordes, director`) with a uniform `tick(state, ctx)`, a `tickWorld` that folds them in canonical order, and an **`advanceWorld(state, hours, graph?)`** that moves the tracked world slices forward with **no player action** — deterministically and save-losslessly. The fixed 14-stage pipeline is untouched in shape: each world stage's body now delegates to its layer (stage 7 → regions, stage 6 → noise+zombies, stage 8 → weather+timeOfDay, stage 9 → hordes, stage 11 → director), so the invariant order assertion stays green and behaviour was byte-identical the moment it landed. 11 engine tests (layer independence, the six-in-order registry, off-screen advance, determinism, save round-trip, zero-hour inertness).

- **Off-screen regional drift — a region festers while you're elsewhere (M2 · T24 · FR-SIM-03)** (`prototype/engine/src/sim/regionDrift.ts`): the `regions` layer now evolves `threat` and `zombieDensity` every tick before the T17 loot contest. Density relaxes toward a carrying capacity set by the region's own `threat` (breeds) and `survivorActivity` (culls); threat tracks that density and any active fire and bleeds off as the dead disperse — a coupled, integer-clamped relaxation with a tiny per-region `region`-stream jitter so no two regions move in lockstep. It can only ever move a value *toward* a clamped 0–100 target, so it never manufactures an impossible state. **The DoD holds: a region's threat measurably changes across days with the player absent** (verified 35→6 across a live run driven only by `advanceWorld`). No `SAVE_SCHEMA_VERSION` bump — the fields already existed. 9 tests.

- **Zombie state machine + first distinct types (M2 · T25 · FR-CBT-06/07)** (`prototype/engine/src/sim/zombies.ts`): the walkers loitering at a node are now **simulated agents with states** — dormant · wandering · investigating · chasing · feeding · hibernating — that snap up an arousal ladder on senses (the T14 node noise, the player's presence, a bleeding player's scent, the phase) and relax one rung per quiet tick, diverting onto corpses to *feed*. The first two **types** ship as content read for behaviour: the **Screamer** rouses its neighbours (deposits noise into adjacent nodes so the alarm cascades) and the **Stalker** hunts hard toward chasing at night when the player is near. New `NodeState.zombieState` + `zombieTypes` ⇒ **`SAVE_SCHEMA_VERSION` 2 → 3** with a forward-only `migrateV2toV3` rung (every node defaults to `dormant`/`[]`), mirroring the T15 v1→v2 rung. New `content/zombies/{walker,screamer,stalker}.json` + `zombie.schema.json`, a `zombieTypes` property on the node schema, and a screamer seeded at the clinic and a stalker at the marina. Schema gate green (5 types, 15 entries). 11 engine tests + a v2→v3 migration test.

- **Migrating hordes that re-path to noise (M2 · T26 · FR-SIM-07/FR-CBT-08)** (`prototype/engine/src/sim/hordes.ts`): `GameState.hordes` (shaped since T3, never populated) now carries a live, roaming mass — one starter horde seeded per run, away from the start node. Each tick a horde scans the nodes within its `awareness` hops and **re-paths toward the loudest one that clears the re-path bar** (a logged gunshot at `FIRE_NOISE` 75 redirects it; ordinary footsteps at 8 do not), then steps toward its destination along a graph shortest-path — so it turns the turn a shot is fired and *arrives over the following turns*. With no stimulus it migrates to a wander destination drawn from the named `horde` stream. A horde is never a combat target (FR-CBT-08) — the systemic handle is noise, not hit points. 11 tests including the gunshot-re-path rate DoD and a property that a horde never lands on a non-node.

- **Weather with multi-system effects (M2 · T27 · FR-SIM-05)** (`prototype/engine/src/sim/weather.ts`): `World.weather` now **transitions over time** on a sticky, reproducible walk (clear · cloudy · rain · storm · fog · snow · wind) and each state carries an effect vector that **touches several systems at once** — a stealth-detection delta wired into the T15 `detectChance` (rain/fog/storm cut visibility and make you harder to spot; snow/wind help the dead), grid power pressure, and regional road pressure. So a storm re-labels the sky *and* drains `powerGrid` *and* degrades every region's `roads` — one change, three systems — while infrastructure only ever falls (GDD IV). The `detectChance` change is backward-compatible (M1's two-arg calls stay clear-equivalent). No schema bump. 8 tests.

- **Survival pressure — the loop bites now (M1 · T22, loop-feel tuning pass)** (`prototype/engine/src/sim/survival.ts`): the Loop-Feel read said choosing move/search/rest was frictionless — needs drifted too softly to threaten and a wound cost nothing. T22 makes the Survival Triangle pull on every turn, strictly on the loop (no world reactivity — that stays M2). **Needs bite and can be fed:** hunger/thirst/fatigue climb every hour (thirst the sharpest at 2/hr), and **`eat`/`drink` spend a scavenged ration/water** to buy a need back down — which is *why* the pack matters. **Wounds decline you while untreated (FR-INJ-04):** every open wound tires you faster and a **bite drives an infection** (`infection.progression`) that stages incubating→symptomatic→terminal and, at terminal, kills — the ticking clock the clinic's antiseptic/antibiotics exist to stop; **`treat`** spends the right medical item to advance a wound's care (T16 `treatWound`), and closing the bite halts the infection. **Neglect ends the run:** maxed hunger/thirst or a terminal infection is a real, *derived* death (`runEndReason`, no new stored state ⇒ no `SAVE_SCHEMA_VERSION` bump) — so search-vs-drink-vs-treat-vs-push-on is a live trade. A **modest starting kit** (2 water, 2 food) buffers the opening so the clock is pressure, not a guillotine; a careful survivor roughly doubles a heedless one's lifespan before the finite region runs dry. `eat`/`drink`/`treat` are surfaced only when relevant (need pressing / wound open + the item carried), so they never clutter the single-decision screen. Pure, deterministic, dependency-free, integer-only. **12 engine + 3 harness tests** (needs drift + rest recovery, eat/drink relief + consumption + offer-gating, infection staging, an untreated bite that climbs to a lethal terminal, treatment that halts it, each run-end cause, an ended run offers no actions and narrates the death, determinism + save round-trip, and a Rivermouth run where managing needs outlasts neglecting them). Engine 146 + harness 39 + content-loader 9 green; schema gate green; the survival-aware 100-turn audit stays violation-free. Re-approaches the M1 Loop-Feel Check with a loop that has stakes turn to turn.

- **Lossless quit/resume at any turn boundary + the M1 slice, end to end (M1 · T21)** (`prototype/harness/src/play.ts` resume seam, `src/playCli.ts`, `test/resume.test.ts`): the last M1 Definition-of-Done proof — a player can stop after *any* turn and resume with nothing lost. The client gains a save/resume seam over the T7 `SaveFile` string (the engine still does zero I/O; the client owns storage, ADR-0003): `saveState` serializes the run, `resumeSession` reconstructs it from the string **alone** — the transient region graph is rebuilt from content, never stored (as since T11) — and continues. The proof is the T7 round-trip elevated from a single state to the whole play sequence: for a scripted slice, saving after turn *n*, reconstructing, and finishing is **byte-identical to never having stopped, asserted at *every* boundary**, across turn types **including a mid-combat boundary** (`combat` is now in state). A new interactive, keyboard-only CLI (`npm run play`, `npm run play -- --resume <file>`) drives the pure core through a pipe-safe read-render-resolve loop and writes the save to a client-owned file. A **full M1 slice** runs end to end over shipped Rivermouth exercising every Part 1–3 system — move, search under the weight cap, deplete/loot, wound, fight-or-slip, rest — with every resolved turn passing the T13 no-op-turn audit and every state safe to quit. **4 harness tests** (resumable at every explore boundary, resumable mid-combat, the save string as sole handoff to a fresh graph, and the full-slice end-to-end audit+lossless run). Harness 36 green; engine 133 + content-loader 9 green; schema gate green. This closes M1's build; the **Loop-Feel Check (§4 Stage-1 gate)** — the owner's verdict on whether choosing an action is interesting — is the only remaining M1 exit criterion and is a human call (recorded in the tracker's Fun-Gate log). A pass opens M2; a fail keeps scope on the loop.

- **Accessibility baseline — keyboard-only, all-critical-info-in-text (M1 · T20)** (`prototype/harness/src/play.ts`, `test/accessibility.test.ts`): accessibility is a Must **from the first UI**, not an M5 retrofit (PRODUCTION §10), and the first client is text end to end — the strongest starting point. **NFR-ACC-01** (no color/audio-only meaning): the render emits **zero ANSI escape codes** — every critical fact is carried in words — a full pack states the leave-behind ("full; you'll have to leave things behind") and offers a drop, an active wound is *named* in prose ("A laceration on your left arm is untreated."), a threat states both the danger and the way out ("2 walkers shamble here… or slip away"), and every choice line shows its known cost; a plain-text transcript contains everything needed to play. **NFR-ACC-02** (semantic, navigable structure): the screen decomposes into a fixed region order (`SCREEN_REGION_ORDER`: header → status → story → prompt → choices → footer) that is **stable across turn types** (explore / over-weight / wounded / combat), and play is **entirely keyboard-driven** — `parseCommand` maps a typed number to its choice (S saves, Q quits) with nothing reachable only by pointer or timing, and `playByInputs` proves a run of number keys plays the identical slice as the equivalent choice script. **9 harness tests**. Harness 36 green. The `describe*`/`renderRegions` seam is the single source a future screen-reader view reads from.

- **Story-first single-decision client — the first real playable (M1 · T19)** (`prototype/harness/src/play.ts`, `test/play.test.ts`): the M0 empty-turn harness becomes the **first real client** of the engine, presenting the whole M1 loop as a short scene that resolves to one decision rather than a grid of bars. `renderScene` lays the screen out in the fixed **header → status → story → choices → footer** order (FR-UI-01); only critical state shows, and only in words — needs are prose ("You are hungry and tired."), the pack is a plain count, wounds are named, and **infection is symptoms, never a number** (FR-UI-02); every listed choice advertises its **known** time cost but hides its outcome, and there are **no fake choices** — an unoffered submission is rejected (FR-UI-03); the layout is a single top-to-bottom column, the mobile-first/one-hand shape later clients inherit (FR-UI-05). The pure `playSession` folds chosen choice-ids into resolved turns and `transcript` renders a whole run as readable narrative, so a Vitest test drives exactly what a human sees. The engine is untouched — a client-only task (DESIGN §3) — save one cleanup: the pack's precise count moved out of engine narration (which now surfaces only the qualitative "Your pack is full.") into the client's status line, keeping the engine scene world-focused. **6 harness tests** (screen-shape/region order, only-critical-stats + no infection number, no-fake-choices rejection, a multi-turn Rivermouth scavenge that reads as narrative and audits clean each turn, and render determinism). Harness 23 green at landing. Feeds T20 (accessible phrasing) and T21 (the session is what quit/resume saves).

- **Weight-limited inventory — the pack that forces a leave-behind (M1 · T18)** (`prototype/engine/src/sim/inventory.ts`): the T17 loot economy made loot finite; T18 makes **carrying** it finite too, and only then does scavenging bite. The pack has an integer weight budget (`CARRY_CAPACITY` = 40); every item the loot tables can emit has a weight (`ITEM_WEIGHTS` — light consumables 1, food/water 3, bulky gear 6, a firearm 8; unknown ids default to 2); and `inventoryWeight` never exceeds the budget. `addItemBounded` pockets a find **only if it fits**, returning `{ inventory, carried }`; when it does not fit the item is **left in the world**. This closes the FR-ECO ↔ FR-PLR loop at the one point they meet: `resolveSearchLoot` now debits the region's finite `loot` **only for what was actually carried**, so a full pack literally stops you draining the well — scavenging is self-limiting, not a vacuum. `dropItem` is the leave-behind lever (remove one unit to reclaim weight), surfaced as a `drop` action offered per carried stack in `availableActions` — a 0-hour management move that changes the pack without advancing the clock, and the Scene now reads the pack ("Your pack is heavy (X/40)." / "Your pack is full."). Carry weight is **derived, never stored**, so there is no state-shape change and **no `SAVE_SCHEMA_VERSION` bump** — nothing new to migrate. Item weights are engine constants for M1 (a bridge until the item content set lands in M2, as the T17 loot-table ids are today). Pure, deterministic, dependency-free, integer-only. **11 engine tests** (weights + default, summed weight + remaining capacity, bounded-add fits/overflows leaving the reference untouched, drop reclaims weight / drops the last unit / inert on an empty stack, a search-heavy run never overfills, a full pack no longer debits region loot, a pipeline drop reclaims weight without advancing the clock, save round-trip, and a fast-check property that no sequence of searches ever overfills the pack) + the harness loot run still green. Engine 133 + harness 17 green. Retires FR-PLR-03. Feeds T19 (the Scene narrates the find and the leave-behind).

- **Finite, contested, depleting loot economy (M1 · T17)** (`prototype/engine/src/sim/loot.ts`, node `kind` on `content/nodes/`): loot is a **stock, not a spawn**. A region carries a finite `RegionState.loot` richness (0–100); **searching draws from it and debits it** (FR-ECO-01), and the yield is **partial and diminishing** — capped by how thin the region is and how picked-over the node is (`searchYieldCap`, FR-ECO-03) — so a search returns a portion, not a jackpot, and the concrete item is drawn from a **plausibility table keyed to the node's kind** (a clinic yields medical, a store yields food, the police post yields ammo; FR-ECO-02). The world **contests** the same stock: pipeline **stage 7 (`updateRegion`)** now thins every region a little each turn in proportion to its `survivorActivity` (rivals working off-screen), so a node you leave unsearched can be poorer when you return — and the six shipped Rivermouth nodes gained a `kind` so the district loots plausibly. Loot only ever falls and is clamped at 0, so a run can never pull more than a region held; a depleted region yields nothing but time and noise. Deterministic (named `loot` RNG stream), pure, dependency-free, integer-only. **10 engine tests** (kind→table routing + generic fallback, partial/diminishing caps, a search debits the region and packs a plausible item, a dry region yields nothing, region loot monotonically falls over a run, rivals thin a region with no searching, zero-hour/clamp inertness, determinism, save round-trip, and a fast-check property that no run pulls more than the region started with) + **1 harness integration run** over shipped Rivermouth (a scavenging run fills the pack while the stock only falls, lossless each turn, zero audit violations). Engine 122 + harness 17 green; content gate green over enemies + nodes + regions + wounds. Feeds T18 (weight forces leave-behind) and T19 (the Scene narrates the find).

- **Avoidable combat, loud firearms & a stealth path (M1 · T15)** (`prototype/engine/src/combat/combat.ts`, `content/enemies/`, `content/schemas/enemies.schema.json`): the loop can now *bite*, but only if you choose it. Walkers loiter at a node (new `NodeState.walkers`, node memory); a contested node offers an **avoidable** encounter — **Fight** (melee: 1–2 dmg, a swing of noise, and an alerted walker that may land a **named wound** via T16), **Fire** (only when a loaded firearm is carried: one-shots a walker, spends a round, and dumps region-scale noise via the T14 `params.noise` override — `FIRE_NOISE` 75 ≫ `MELEE_NOISE` 15, FR-CBT-04), and a **Slip away** to any discovered neighbour (FR-CBT-05, the DoD: a detection roll over the node's current noise + day phase decides a clean escape vs. a parting wound, but you *always* get out — a stealth-only survivor can cross the whole region without ever entering combat). A started fight is `GameState.combat` (nullable), carried across turns so each Strike/Fire/Retreat is one resolved, turn-based exchange (FR-CBT-02); killing the walker clears the fight and drops the node's count. Every option debits a real resource (FR-CBT-01): time, noise, ammo, or the risk of a wound. All rolls come from named RNG streams (`combat`, `stealth`), so a seed reproduces a fight exactly. State-shape change ⇒ **`SAVE_SCHEMA_VERSION` 1→2** with one forward-only migration rung (old saves gain `combat: null` and every node `walkers: 0`), and `combat` joins `TRACKED_SYSTEMS`. Stage-1 validation now covers any Scene choice (by `choiceId`), so combat actions are validated like the rest; a bare `wait` still passes inertly. **12 engine tests** (encounter offered + firearm-gated fire, stealth escapes and never forces combat, turn-based kill drops the walker, firearm louder than melee + spends ammo, determinism, named-wound retaliation + mid-fight save round-trip, `detectChance` monotonicity, v1→v2 migration, and an escape-always-offered property) + **3 harness integration runs** over a hand-built district (slip past, fight to the death autosave-lossless each swing, one loud shot). Engine 112 + harness 16 green; content gate green over enemies + nodes + regions + wounds. Unblocks T17 (loot on a searched node; contested by the world) and T19 (the Scene narrates the fight).

- **Named wounds — treated, not regenerated (M1 · T16)** (`prototype/engine/src/sim/wounds.ts`, `content/wounds/`, `content/schemas/wound.schema.json`): damage is no longer a shrinking bar but a discrete, **named** wound (FR-INJ-01). A new content type names the vocabulary — `wound.laceration` (bleed), `wound.sprain` (slow), `wound.bite` (infect-risk), `wound.fracture` (weaken) — each with a baseline severity, a gameplay `effect`, and the items that treat it; the schema gate validates it like all content (FR-CNT-02). A live wound is a `Wound` in `player.condition.wounds` carrying the *type id* (never a copy), the body site, its severity, accumulated treatment, and the day it was inflicted; it persists across turns and saves. The module is built around the FR-INJ-04 invariant — **health is treated, not auto-regenerated**: `inflictWound` appends (wounds accumulate, they never merge into a bigger number), and **only** `treatWound` — spending care — advances a wound's `treated`, with a wound leaving the body solely when its care reaches its severity. Nothing in the passage of time lowers a wound. `woundBurden` (the summed untreated remainder) is the handle combat (T15), the needs drift, and the Scene (T19) will read. Pure, deterministic, dependency-free, integer-only. **13 engine tests** — named-field infliction, accumulation, worst-wound + burden, save round-trip, treat-advances-then-closes, site-filtered and worst-first targeting, zero-care inertness, and two anti-regen proofs (playing move/search/rest with a wound present never lowers it; a fast-check property that over any no-treatment play the wound multiset never shrinks). Engine 100 tests green; content schema gate green over nodes + regions + wounds. Unblocks T15 (an enemy blow that lands inflicts a named wound).

- **Noise deposit model — the FR-SIM-06 sound layer (M1 · T14)** (`prototype/engine/src/sim/noise.ts`): loud actions now leave sound behind in the place they were made, and time carries it off. Pipeline **stage 6 (`updateNode`)** stops being an identity no-op: each turn it **decays every node's noise** by the hours the action spent (`NOISE_DECAY_PER_HOUR` = 5/h, floored at 0) and then **deposits** that action's noise into the node the player now stands on — decay-*before*-deposit, so a just-made sound stays at full strength for the next turn's read (the "gunshot this turn → horde next turn" timing of DESIGN §5; the horde consumer lands in M2). Volume is legible and ranked: `rest`/`wait` are silent (0), `move` is `8` (rubble underfoot), `search` is `25` (rummaging carries), and an action may override with an integer `params.noise` — the hook firearms use to be region-loud in T15. Noise lives in the existing `NodeState.noise` (0–100 int); the module is pure, deterministic, dependency-free and integer-only, and a silent zero-cost `wait` returns the state untouched, preserving the M0 empty-turn contract. **DoD proven — the quiet path is legibly quieter:** a rummaging survivor leaves the district materially noisier than a resting one over the same horizon. **14 engine tests** (per-action volume + the firearm override, decay/deposit ref-stability and clamping, stage-6 deposit-at-destination, rest-decays-a-prior-sound, integer + save round-trip, the loud-vs-quiet DoD, and a fast-check bound property) + **2 harness integration runs** over shipped Rivermouth (a loud/quiet A/B that stays autosave-lossless, and a 30-turn run whose every resolved turn still passes the T13 audit). Engine 92 + harness 13 green. Unblocks T15 (firearms deposit region-scale noise; stealth detection reads it).

- **Per-turn change telemetry — the FR-CORE-04 no-no-op-turn audit (M1 · T13)** (`prototype/engine/src/telemetry/turnAudit.ts`): the Definition-of-Done invariant of the core loop — **every resolved turn moves at least one real system** — is now proven by machine, not by hand. A new telemetry module diffs a turn's before/after `GameState` **by value** (`jsonEqual`, an honest plain-JSON deep-equal that doesn't mis-report a pure stage re-allocating an identical slice) and reports which slices changed (`diffSystems` → `auditTurn` returning `{turn, resolved, changedSystems, ok}`). The tracked systems deliberately **exclude `meta`**: counting the always-advancing clock/turn counter would make the invariant vacuously true, so the audit asks the harder question — did anything change *besides* time? `applyAction` is instrumented to return the `changed` systems on every `TurnResult` (empty for an inert `wait`, which resolves no turn). Pure, deterministic, dependency-free. **12 engine tests** (by-value comparison, `meta` exclusion, per-action change reporting, a fast-check property that every resolved turn over random play changes ≥ 1 system, and a hand-built clock-only turn proving the audit reports `ok: false`) plus a **harness 100-turn telemetry audit** over the shipped Rivermouth region — all 100 resolved turns changed ≥ 1 system, zero violations (PRD §6.1 acceptance criterion). Engine 79 + harness 11 tests green. Feeds T14 (noise as a per-turn node-memory change) and the T21 Loop-Feel gate.

- **Core action loop — move / search / rest with time cost (M1 · T12)** (`prototype/engine/src/time/`, `src/actions/`, `src/pipeline/`): the pipeline's player-facing stages are now real. A new **world clock** (`phaseOf` + `advanceClock`) turns each action's hour cost into advancing time — rolling hour → day → phase and ticking the turn counter — so **time always advances** (FR-CORE-03; move 2h, search 3h, rest 6h). `applyAction(state, action, graph?)` threads the transient region graph and wires the stages: **validate** rejects an action the current node didn't offer (`IllegalActionError`, FR-CORE-01), **resolvePlayerAction** applies the effect — move relocates + visits + lifts fog around the destination, search advances the node's `searchPct` (persistent node memory), rest recovers fatigue — **updatePlayer** drifts hunger / thirst / fatigue by the hours spent, and **generateScene** (`sceneOf`) renders the Four Questions (FR-CORE-05): where you are, what's happening, and what you can do as costed choices with hidden outcomes. The zero-cost `wait` stays inert, so every M0 skeleton test still passes unchanged. Pure, deterministic, integer-only, and **autosave-lossless after every turn** (FR-CORE-07). 14 new engine loop tests (clock, move/search/rest, validation, invariants) + 2 harness integration turns played over the shipped Rivermouth region. Unblocks T13 (per-turn change telemetry), T14 (noise), and T17 (loot yield on search).

- **Region & node graph with fog of war + node memory (M1 · T11)** (`prototype/engine/src/map/`, `content/nodes/`, `content/regions/region.rivermouth.json`): the first real region — **Rivermouth District**, six nodes wired into a ring so scouting the start reveals two neighbors and the rest stay fogged. The node graph is **content**: a new `node` schema + one file per node, with the engine's `buildRegionGraph` enforcing the cross-file integrity JSON Schema can't — symmetric routes, exactly one start node, full connectivity, and known region/node references — throwing `MapError` at run start otherwise. `NodeState` gains a `discovered` fog-of-war flag (FR-MAP-02); *visited* stays `lastVisit !== null`, so a node can be known-but-unentered. `discoverAround` reveals a node and its neighbors; `startRun` bootstraps a playable run — seeds live `regions`/`nodes` from content baselines, stands the player on the start node (visited today), and lifts the fog around them. Pure, dependency-free, integer-only, plain-JSON, deterministic, and save-round-trippable. 21 engine tests (integrity failures, reveal, seeding, a fog property) + 3 harness integration tests over the shipped content. Retires the throwaway `region.test-downtown`. FR-SIM-02, FR-MAP-01..03,06; unblocks T12 (move/search/rest over the graph).

- **Terminal harness — runs an empty turn (M0 · T9)** (`prototype/harness/`):
  `@zurvival/harness`, the first headless client of `@zurvival/engine`. `runEmptyTurn()`
  creates a run, resolves one turn through the pipeline, renders the `Scene`, and asserts the
  two M0 exit proofs — byte-identical determinism and a lossless save round-trip. Runnable via
  `npm start` (tsx); 5 Vitest + fast-check tests.
- **CI + content schema gate (M0 · T8)** (`.github/workflows/ci.yml`,
  `prototype/content-loader/src/validateCli.ts`): a merge-blocking gate that loads the whole
  `content/` tree through the loader and exits non-zero on any malformed file (bad JSON,
  schema violation, duplicate id, or a populated type with no schema). CI typechecks and tests
  every package, runs the harness empty-turn smoke check, and proves the gate rejects
  deliberately malformed content (FR-CNT-02 — volume can never corrupt a run).
- **Save / load — versioned lossless round-trip (M0 · T7)** (`prototype/engine/src/save/`):
  a `SaveFile` envelope (format + save-schema version + one-line "where you are" summary +
  state) with a migration-ladder hook (empty at v1) and `SaveError` on corrupt, foreign, or
  future-versioned blobs. `saveGame`/`loadGame` deep-equal round-trip including `rng`,
  `history`, and `queue`; pure, clock-free, and part of the dependency-free engine core. 10
  tests (QA TC-DET-05 / TC-DET-07).
- **Content loader + first schema (M0 · T6)** (`prototype/content-loader/`):
  `@zurvival/content-loader` — a separate Ajv-backed package (never imported by the
  dependency-free engine) that validates `content/` against JSON Schema (2020-12), indexes
  entries by id, and reports every issue together. First `region` schema + one throwaway test
  region. 9 tests.
- **Seeded RNG with named streams (M0 · T5)** (`prototype/engine/src/rng/`): `sfc32` seeded
  via `cyrb128`; each named stream (`loot`, `encounter`, `combat`, …) is a pure function