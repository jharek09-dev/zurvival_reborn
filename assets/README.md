# Assets

Placeholder for production assets and references. Nothing here is final.

- `audio/` — music themes, ambience, signature sounds, and radio audio (see GDD Part XVII).
  Large binaries should be tracked with Git LFS once the pipeline is set up.
- `art/` — concept art, UI mockups, key art, and the visual identity references
  (see GDD Part XVI). The prior remodel GDD's embedded images can be extracted here.

Keep source files (project files, layered art) with any exported deliverable where size
allows.
